# 分镜视频生成流程：阶段任务与验收标准

## Summary

目标是把“小说/剧本 → 分镜表 → 参考图 → 分镜图 → 分镜视频 → 成片序列”做成可生产的无限画布工作台。

执行顺序分 5 个阶段。每个阶段必须单独验收，不要一次性大改。当前已知阻塞是前后端生成任务字段不一致，必须先修。

方案存档文件：`docs/分镜视频生成流程方案.md`  
每个阶段完成后，在该文档追加验收记录。

## Phase 0：修通真实生成任务基础链路

### 任务

- 修复前端生成任务请求字段：
  - 从 `bookId / shotId / sourceNodeId / aspectRatio / durationSeconds / simulateError`
  - 改为 `book_id / shot_id / source_node_id / aspect_ratio / duration_seconds / simulate_error`
- 确认 `generate-image` 和 `generate-video` 都能返回 `task_id`。
- 确认任务轮询 `GET /api/prototyping/tasks/{task_id}` 能拿到 running/done/error。
- 确认生成成功后资产写回 `StoryboardShot.asset_links`。
- 确认刷新页面后真实资产能从 `/api/pipeline/book/{book_id}/outputs` 回填到画布。

### 验收标准

- `npm run test` 通过。
- `npm run build` 通过。
- 浏览器实测：
  1. 打开真实项目。
  2. 选择一个镜头。
  3. 点击“生成图片”。
  4. 图片节点先显示“生成中”，后显示“已完成”。
  5. 点击图片节点“生成视频”。
  6. 视频节点生成成功，并连接到成片序列。
  7. 刷新页面后图片、视频、采用状态仍存在。
- 不允许再出现 `/api/prototyping/generate-image` 或 `/api/prototyping/generate-video` 的 422。

## Phase 1：画布表达完整生产流程

### 任务

- 把当前画布从“镜头 → 图片 → 视频”扩展为三层结构：
  - 文本层：剧本、分镜表。
  - 参考资产层：人物定妆、场景、道具、参考图。
  - 镜头生产层：分镜图、分镜视频、成片序列。
- 增加“参考资产层”开关：
  - 默认收起，只看镜头生产主线。
  - 展开后显示人物、场景、道具、参考图与镜头的关系。
- 镜头卡片增加生产状态：
  - 参考图完整度。
  - 分镜图状态。
  - 视频状态。
  - 是否已进入成片序列。
- 右侧面板按节点类型显示不同操作：
  - 人物/场景/道具：生成参考图。
  - 镜头：生成分镜图。
  - 分镜图：生成视频。
  - 视频：采用版本。

### 验收标准

- 打开真实项目后，用户能一眼看出：
  - 哪些镜头还缺分镜图。
  - 哪些镜头还缺视频。
  - 哪些镜头参考图不完整。
  - 哪些视频已经进入成片序列。
- 展开参考资产层后，能看到：
  - 人物定妆连接到相关镜头。
  - 场景连接到相关镜头。
  - 道具连接到相关镜头。
- 收起参考资产层后，画布不拥挤，主线仍清晰。
- 老的导演模式、高级模式不受影响。

## Phase 2：参考图生成与回填

### 任务

- 为人物定妆、场景、道具支持生成参考图。
- 参考图仍使用 `image` 节点，但增加用途标识：
  - `imageRole: "reference"`
  - `assetScope: "character" | "location" | "prop"`
  - `assetSubject`: 角色名 / 场景名 / 道具名
- 后端生成参考图后写回 `asset_links.references`：
  - `references.characters`
  - `references.scene`
  - `references.props`
- 参考图必须能作为后续分镜图和视频生成的输入资产。

### 验收标准

- 选择人物定妆节点，点击“生成参考图”，成功后出现人物参考图节点。
- 选择场景节点，点击“生成参考图”，成功后出现场景参考图节点。
- 选择道具节点，点击“生成参考图”，成功后出现道具参考图节点。
- 参考图节点显示“真实资产”标签。
- 刷新页面后参考图仍存在，并仍连接到对应人物/场景/道具和相关镜头。
- 如果参考图生成失败，节点显示失败原因，并可重试。

## Phase 3：分镜图生成使用参考资产

### 任务

- 生成分镜图时，不再只传镜头静态提示词。
- 请求必须包含：
  - 镜头静态提示词 `visual_prompt_static`
  - 当前镜头关联的人物参考图 asset id
  - 当前镜头关联的场景参考图 asset id
  - 当前镜头关联的道具参考图 asset id
- 如果参考图缺失，允许继续生成，但 UI 必须明确提示“一致性风险”。
- 分镜图写回 `asset_links.images`。
- 分镜图节点使用：
  - `imageRole: "storyboard"`
  - `assetScope: "shot"`

### 验收标准

- 对有完整参考图的镜头生成分镜图，请求体包含 reference asset ids。
- 对缺少参考图的镜头生成分镜图，页面给出风险提示，但仍可继续。
- 分镜图生成成功后：
  - 连接来源镜头。
  - 显示引用了哪些人物/场景/道具参考图。
  - 默认成为当前镜头采用图片版本。
- 多次生成会新增版本，不覆盖旧版本。
- 刷新页面后所有分镜图版本和采用状态仍存在。

## Phase 4：视频生成使用分镜图 + 运动提示词 + 参考资产

### 任务

- 视频生成必须从分镜图节点发起。
- 请求必须包含：
  - 分镜图片 asset id
  - 分镜运动提示词 `visual_prompt_motion`
  - 镜头 duration
  - 人物/场景/道具参考图 asset ids
- 视频生成成功后写回 `asset_links.videos`。
- 视频节点必须连接：
  - 来源分镜图
  - 成片序列
- 采用视频版本时，同一镜头只能有一个 adopted video。

### 验收标准

- 选择分镜图节点，点击“生成视频”。
- 视频任务进入 running，完成后变为 done。
- 视频节点显示：
  - 来源分镜图。
  - 使用的运动提示词。
  - 使用的参考资产数量。
  - 真实 asset id。
- 新视频默认进入成片序列。
- 切换采用版本后：
  - 旧视频仍保留。
  - 旧视频不再进入成片序列。
  - 成片序列输入数量和连线正确。
- 刷新页面后视频版本和成片序列关系仍正确。

## Phase 5：成片序列与验收级体验打磨

### 任务

- 成片序列节点显示当前集所有已采用视频：
  - 镜头顺序。
  - 视频版本。
  - 时长。
  - 缺失镜头。
- 增加“播放预览”面板：
  - 当前可以先展示视频列表和缩略图，不要求真正拼接视频。
- 增加批量状态概览：
  - 总镜头数。
  - 已有参考图镜头数。
  - 已有分镜图镜头数。
  - 已有视频镜头数。
  - 已进入成片序列镜头数。
- 增加错误队列：
  - 哪些任务失败。
  - 失败原因。
  - 重试入口。

### 验收标准

- 用户能从顶部或右侧快速判断整集生产进度。
- 成片序列能准确列出所有 adopted videos。
- 缺失视频的镜头必须明确标红或提示。
- 播放预览不再只是跳到节点，而是展示当前成片序列内容。
- 任一任务失败不会导致白屏。
- 失败任务可以重试。
- 项目切换、集数切换后状态不串。

## Cross-phase Test Requirements

每个阶段完成后都必须执行：

- `npm run test`
- `npm run build`
- 至少一次真实项目浏览器验收。
- 更新 `docs/分镜视频生成流程方案.md` 的验收记录。

最终总体验收必须覆盖：

1. 打开真实项目。
2. 选择第 1 集。
3. 生成人物参考图。
4. 生成场景参考图。
5. 生成道具参考图。
6. 选择一个镜头生成分镜图。
7. 选择分镜图生成视频。
8. 采用视频版本。
9. 查看成片序列。
10. 刷新页面。
11. 确认所有资产、连线、采用状态完整回填。

## Assumptions

- 第一版仍使用后端“原型任务适配层”生成占位真实资产，不直接接入商业生图/视频服务。
- 不重做旧导演模式和高级模式。
- 不新增数据库表，优先复用 `StoryboardShot.asset_links`。
- 如果执行中发现 `asset_links` 难以维护参考图结构，再单独提出数据库迁移方案，不在本轮直接扩表。
- 参考图、分镜图、视频都必须保留多版本，禁止覆盖旧结果。
