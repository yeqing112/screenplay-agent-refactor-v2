# 阶段四验收记录

日期：2026-06-25

项目：`神农架历险记 / 第 1 集`

## 结论

阶段四核心能力已基本落地，当前版本可以认为：

> 已完成“整集批量生产、失败任务中心与导出前检查”的主功能实现，并通过工程验证与一轮真实项目浏览器抽查。

本轮抽查中，`神农架历险记 / 第 1 集` 已处于完整状态，因此没有再出现“缺分镜图 / 缺视频”场景，批量按钮按产品预期未显示。这说明检查面板在“问题驱动”模式下能自动收敛，但也意味着：

> 本次浏览器抽查验证了入口、状态判断、导出结论、失败中心与刷新回填；未在浏览器里再次覆盖“缺失镜头上的批量生成请求发出”这一条动态路径。

这部分目前主要由单元测试和既有真实任务链路兜底。

## 已验证通过

- `npm test -- --run` 通过，当前共 `55` 个测试。
- `npm run build` 通过。
- 真实项目可正常进入原型工作台。
- 顶栏存在并可打开：
  - `检查本集`
  - `失败任务`
  - `导出前检查 / 本集可交付`
- `检查本集` 面板可正常打开。
- `失败任务中心` 可正常打开。
- `导出前检查` 可正常打开。
- 当前整集闭环完成时，顶栏会显示 `本集可交付`。
- 导出弹层标题会显示 `本集可导出 / 可交付`。
- 刷新页面后状态保持正常。
- 本轮自动化抽查未发现：
  - 页面运行时错误
  - 业务接口 4xx / 5xx
  - 控制台 warning

## 本轮抽查状态

抽查时页面显示：

- 镜头：`8`
- 分镜图：`8`
- 视频：`8`
- 成片：`8`

因此：

- `缺分镜图镜头` 数量为 `0`
- `缺视频镜头` 数量为 `0`
- 批量生成按钮未出现，这是符合当前数据状态的

## 本阶段新增能力

- 本集检查面板：
  - 缺分镜图
  - 缺视频
  - 未采用视频
  - 未进入成片
  - 风险镜头
- 批量生成入口：
  - 为缺分镜图镜头批量生成分镜图
  - 为缺视频镜头批量生成视频
- 失败任务中心：
  - 失败聚合
  - 重试
  - 查看节点
  - 标记已处理
- 导出前检查：
  - 基于整集状态、失败任务、成片覆盖率、本地保存状态综合给出是否可交付
- 发布级小修补：
  - 补充 favicon，减少静态资源 404 噪声

## 仍建议补做的回归抽查

虽然阶段四主能力已经到位，但为了把“验收结论”从“基本通过”推到“完全通过”，仍建议补一轮人工或自动化回归，刻意选择一个“不完整集”或临时制造缺口状态，复验这几条：

- 至少 2 个缺分镜图镜头时，检查面板出现批量分镜图按钮。
- 至少 2 个缺视频镜头时，检查面板出现批量视频按钮。
- 点击批量按钮后，分别发出独立真实请求。
- 若制造失败任务，失败中心能看到并重试恢复。

## 相关产物

- [result.json](D:/projects/screenplay-agent-refactor-v2/artifacts/acceptance-qa-stage4-flow/result.json)
- [after-batch-actions.png](D:/projects/screenplay-agent-refactor-v2/artifacts/acceptance-qa-stage4-flow/after-batch-actions.png)
- [after-refresh.png](D:/projects/screenplay-agent-refactor-v2/artifacts/acceptance-qa-stage4-flow/after-refresh.png)
