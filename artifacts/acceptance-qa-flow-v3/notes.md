# 分镜视频生成流程复验记录 v3

日期：2026-06-24

## 结论

本轮复验通过。

## 验收路径

1. 打开真实项目 `神农架历险记`。
2. 进入原型工作台第 1 集。
3. 展开参考资产层，选中场景 `树房子前`。
4. 点击 `生成参考图`。
5. 回到镜头列表，选中 `镜头 5`。
6. 点击 `生成图片`。
7. 选中新生成的 `分镜图 5 v1`。
8. 点击 `生成视频`。
9. 打开 `播放预览`。
10. 刷新页面，检查新资产与成片状态是否回填。

## 页面级结果

- 真实项目可稳定进入原型工作台。
- `:4173` 页面下真实任务接口可正常触发，不再出现 `Failed to fetch`。
- 场景参考图任务成功：
  - `树房子前 参考图 v5`
- 镜头图片任务成功：
  - `分镜图 5 v1`
- 视频任务成功：
  - `视频 5 v1`
- 顶部生产统计从：
  - `镜头 8 / 参考 8/8 / 分镜图 2 / 视频 1 / 成片 1`
  变为：
  - `镜头 8 / 参考 8/8 / 分镜图 3 / 视频 2 / 成片 2`
- 刷新页面后：
  - 新分镜图仍存在
  - 新视频仍存在
  - 成片数量仍保持提升后的状态

## 控制台复验

- 未捕获 `Encountered two children with the same key`
- 未捕获 `Failed to fetch`
- 未捕获页面级 `pageerror`

## 备注

- 历史脏数据里的乱码标题已不再以 `?????? 参考图 v1` 形式暴露给页面。
- 仍有少量历史参考图会以兜底文案 `资产 参考图 v1` 展示。这不是乱码，但说明旧数据本身缺少可恢复的 subject，只能做安全回退。

## 截图

- `01-workspace-loaded.png`
- `02-scene-selected.png`
- `03-reference-generated.png`
- `04-shot5-selected.png`
- `05-image-generated.png`
- `06-image-selected.png`
- `07-video-generated.png`
- `08-preview-opened.png`
- `09-after-refresh.png`

## 结果文件

- `result.json`
