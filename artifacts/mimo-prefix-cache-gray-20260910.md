# MiMo 前缀缓存灰度报告（2026-09-10）

## 范围

- 模型：`mimo / mimo-v2.5`
- 请求数：2
- 两次请求使用相同稳定 system 与 user 前缀，仅改变末尾任务句。
- 仅调用文本 LLM；没有写入项目、镜头、资产、Prompt Version，也没有生成图片或视频。

## 供应商实测

| 指标 | 结果 |
| --- | ---: |
| HTTP 成功请求 | 2 / 2 |
| 输入 Token | 4526 |
| 缓存 Token | 2240 |
| 输出 Token | 14 |
| 总 Token | 4540 |
| 累计缓存命中率 | 49.4918% |
| 缓存明细 | 可观测 |

## 判断

本次样本证明：在当前 MiMo 配置下，稳定前缀 + 动态任务尾部的组织方式能够获得供应商返回的真实缓存命中，累计样本命中率约 49.49%。首次请求允许建立缓存，后续请求不应把该比例当作固定承诺；项目/集/版本事实变化时必须使用新缓存分支。

审计只保留模型、供应商主机、指纹、Token 汇总和延迟，不保存密钥、完整提示词或完整响应。下一步可在真实业务 Prompt Compiler 样本上继续观察，但每次仍需显式确认并控制样本数量。
