# AGENT.MD

本文件是 `screenplay-agent-refactor-v2` 的项目级执行规则。后续所有产品重构、前后端实现、联调验收、提示词整改、模型接入、QA 修复与文档更新，都必须遵循本文档。

## 1. 最高优先级文档

项目执行时，以下文档优先级高于零散对话与临时判断：

1. `docs/产品重构蓝图.md`
2. `docs/产品重构实施方案.md`
3. `docs/产品重构阶段任务与验收标准.md`
4. `docs/LibTV化融合重构计划.md`
5. `docs/编码与文本安全规范.md`
6. 本文件 `AGENT.MD`

如对话要求与上述文档冲突，先更新蓝图或阶段任务文档，再改实现。

## 2. 产品定位

本项目统一定位为：

**AI 短剧生产操作系统 / 资产驱动的分镜生成与交付平台**

它不是单一提示词生成器，也不是孤立的小说改编工具。所有功能必须服务完整主链路：

1. 内容准备
2. 改编方向锁定
3. 剧本生产
4. 分镜生产
5. 资产生成与管理
6. QA 修复
7. 导出交付

## 3. 正式产品结构

正式工作台必须围绕以下一级模块组织：

1. 项目控制台
2. 内容准备
3. 改编方向
4. 剧本工作台
5. 镜头工作台
6. 创作画布
7. 资产中心
8. QA 修复
9. 任务中心
10. 导出中心
11. 模型管理

除非蓝图明确要求，不再继续把核心能力散落到旧版入口中。

## 4. 当前执行原则

### 4.1 按蓝图推进，不死磕单点

- 优先推进全局主链路完成度
- 不因单一模块陷入长时间局部深挖
- 每轮工作都要能回答“对整体生产闭环有什么推进”

### 4.2 正式工作台优先

- 新能力优先落到正式产品工作台
- 原型、旧版页面、临时入口只能作为过渡或对照
- 新验收以正式工作台结果为准

### 4.3 真浏览器优先

- 不能只看单测和 build
- 只要涉及真实流程、用户路径、跨模块承接、任务恢复、画布联动，就必须做真浏览器验收
- 验收记录必须落到 `docs/` 下

### 4.4 文档必须同步

- 阶段任务变化要同步到主任务文档
- LibTV 化能力变化要同步到融合计划文档
- 如主文档损坏，先修文档健康，再继续堆增量记录

### 4.5 剧本链路禁止继续走启发式补丁主路径

- 剧本整改不得再把“继续扩写 QA 标题命中规则”当作长期主路径
- 新问题必须优先判断自己属于：
  - 事实层缺口
  - 场景执行层缺口
  - 编译层偏移
- 优先新增结构字段、编译约束、结构 QA，不优先新增样本导向特例规则
- `直接 rewrite 整集剧本` 只保留为兼容回退路径，不再视为生产级主方案

## 5. 提示词与资产规则

### 5.1 分镜提示词

- 静态提示词和运动提示词必须由结构化上下文驱动
- 必须继承权威原文、镜头结构、绑定资产、视觉事实目标
- 不允许回退成空泛、通用、与当前镜头脱节的描述

### 5.2 资产引用

- 人物、场景、道具都允许存在分集默认、镜头精调、阶段变体
- 同一人物在不同集、不同镜头下允许不同造型状态
- 提示词编译必须显式继承当前命中的资产版本与参考图

### 5.3 参考图

- 所有生成的参考图必须可预览
- 参考图要支持删除、状态更新、回写镜头与资产关系
- 恢复链路中的参考图必须能回到正式工作台上下文继续使用

## 6. 模型接入规则

- 模型管理必须支持图片模型、视频模型、能力标签、任务模式与供应商差异
- 新模型接入不能破坏现有任务恢复、状态轮询、输入摘要和验收链路
- 付费模型调用前，优先完成不耗费外部成本的界面、状态、输入链路与回收链路验证

## 7. 编码与文本安全规则

- 所有新文件统一使用 UTF-8
- 一旦文件出现乱码污染，不继续在原文件上堆大段中文 patch
- 优先采用：备份 -> 重建干净版本 -> 回填当前有效内容
- 规则文档、蓝图文档、阶段任务文档属于高优先级健康对象

详细规则见：`docs/编码与文本安全规范.md`

## 8. 验收规则

每项关键改动至少要回答四件事：

1. 改了什么
2. 为什么改
3. 怎么验证
4. 当前还有什么边界或后续任务

关键改动验收优先包含：

- 相关测试
- 最小编译验证
- 全量构建
- 真浏览器路径
- 对应验收记录

剧本链路新增必答项：

1. 这次修改新增了哪个结构字段
2. 它属于事实层、场景执行层还是编译层
3. QA 如何检查这个字段
4. Rewrite / Compiler 如何消费这个字段

## 9. 当前高优先级后续方向

截至 2026-08-03，优先级如下：

1. 继续清理高风险文档与页面文本污染
2. 继续推进正式工作台全链路真浏览器回归
3. 推进创作画布从“关系图”走向“节点式连续创作”
4. 补齐提示词、资产、模型、任务恢复、QA、交付的生产级闭环

## 10. 执行底线

- 不伪造“已完成”
- 不用局部成功掩盖主链路未通
- 不为了赶进度牺牲可维护性
- 不让文档状态落后于真实实现状态
