# 分镜视频生成流程产品化方案

## Summary

把你图里的流程落成“单集短剧视觉生产工作台”：用户不需要理解后端 pipeline，只需要沿着画布上的三层关系推进生产。

核心体验采用“三层画布 + 右侧任务面板”：

- 上层：文本产物，剧本、分镜表、人物画像。
- 中层：一致性资产，人物定妆、场景、道具、参考图。
- 下层：镜头生产，分镜图、运动提示词、分镜视频、成片序列。

方案存档默认路径：`docs/分镜视频生成流程方案.md`。执行阶段创建该文件，并把本计划完整写入，后续随时可查。

## Key Changes

### 1. 画布体验重构

- 保留当前“原型工作台”入口，但把画布从单纯镜头链路升级为“分镜视频生产流”。
- 默认视图只展示用户最关心的链路：`镜头 → 分镜图 → 分镜视频 → 成片序列`。
- 增加“参考资产层”开关，展开后显示：人物定妆、场景、道具、参考图，以及它们如何连接到镜头。
- 每个镜头卡片必须显示：
  - 静态提示词状态：待补充 / 可生成 / 已生成图片。
  - 运动提示词状态：待补充 / 可生成 / 已生成视频。
  - 参考图完整度：人物、场景、道具是否齐全。
- 右侧面板改成“当前节点生产面板”，根据节点类型显示不同操作：
  - 人物/场景/道具：生成参考图。
  - 镜头：生成分镜图。
  - 分镜图：生成视频。
  - 视频：采用版本 / 进入成片序列。

### 2. 数据与节点模型

- 不新增复杂大表，优先复用现有结构：
  - `VisualMakeup / VisualLocation / VisualProp` 表示人物定妆、场景、道具设定。
  - `StoryboardShot.asset_links` 存镜头相关资产。
- `asset_links` 统一扩展为以下结构：
  - `references.characters`: 角色名到参考图资产数组。
  - `references.scene`: 当前场景参考图资产数组。
  - `references.props`: 道具名到参考图资产数组。
  - `images`: 分镜图片版本数组。
  - `videos`: 分镜视频版本数组。
  - `audios`: 音频版本数组，暂时保留。
- 前端 `CanvasDocument` 增加资产角色字段：
  - `imageRole: "reference" | "storyboard" | "video_preview"`。
  - `assetScope: "character" | "location" | "prop" | "shot"`。
- 参考图节点仍使用现有 `image` 节点类型，避免新增过多 node kind；靠 `imageRole` 和标签区分。

### 3. 生成任务接口

- 统一前后端请求字段，前端发送 snake_case，后端继续使用 Pydantic snake_case：
  - `book_id`
  - `episode`
  - `shot_id`
  - `source_node_id`
  - `source_asset_id`
  - `asset_scope`
  - `asset_subject`
  - `target_kind`
  - `prompt`
  - `reference_asset_ids`
  - `aspect_ratio`
  - `duration_seconds`
  - `simulate_error`
- 新增/调整接口：
  - `POST /api/prototyping/generate-reference-image`：生成人物/场景/道具参考图。
  - `POST /api/prototyping/generate-image`：生成分镜图。
  - `POST /api/prototyping/generate-video`：生成分镜视频。
  - `GET /api/prototyping/tasks/{task_id}`：轮询任务状态。
  - `POST /api/prototyping/adopt-version`：保存采用版本。
- 视频生成请求必须包含：
  - 分镜图片 asset id。
  - 分镜运动提示词。
  - 当前镜头关联的人物/场景/道具参考图 asset id。
- 图片生成请求必须包含：
  - 分镜静态提示词。
  - 当前镜头关联的人物/场景/道具参考图 asset id。

### 4. 产品流程

- 用户进入真实项目后，系统自动显示当前集的生产状态。
- 如果人物/场景/道具参考图缺失，镜头卡片显示“参考图不完整”，但允许继续生成，按钮文案提示风险。
- 推荐生产顺序：
  1. 生成人物定妆参考图。
  2. 生成场景/道具参考图。
  3. 生成每个镜头的分镜图。
  4. 用分镜图 + 运动提示词 + 参考图生成视频。
  5. 采用视频版本，进入成片序列。
- 提供“一键补齐参考图”按钮，但默认不自动批量生成，避免用户失控。
- 所有生成结果都新增版本，不覆盖旧版本。
- 所有真实生成资产必须有“真实资产”标签；Mock/模拟结果必须有“Mock”标签。

### 5. 存档与可追踪性

- 执行阶段创建：`docs/分镜视频生成流程方案.md`。
- 存档内容包括：
  - 当前流程图理解。
  - 产品分层设计。
  - 节点和数据结构。
  - API 契约。
  - 分阶段验收标准。
- 后续每个阶段完成后，在同一文档追加“验收记录”小节，避免计划和实际脱节。

## Test Plan

- 单元测试：
  - 真实生成请求字段必须是 snake_case。
  - 参考图资产能归一化为 `imageRole: reference` 节点。
  - 分镜图资产能归一化为 `imageRole: storyboard` 节点。
  - 视频节点能正确连接来源分镜图和成片序列。
  - 参考图缺失时生成请求仍可发起，但 UI 显示风险提示。
  - 采用版本切换后，`asset_links` 中同类资产只有一个 `adopted: true`。
- 后端测试：
  - 生成人物参考图并写回 `asset_links.references.characters`。
  - 生成场景参考图并写回 `asset_links.references.scene`。
  - 生成道具参考图并写回 `asset_links.references.props`。
  - 生成分镜图并写回 `asset_links.images`。
  - 生成视频并写回 `asset_links.videos`。
- 浏览器验收：
  1. 打开真实项目和第 1 集。
  2. 展开参考资产层。
  3. 选择一个人物定妆节点，生成参考图。
  4. 选择一个场景节点，生成参考图。
  5. 选择一个镜头，生成分镜图。
  6. 选择分镜图，生成视频。
  7. 采用视频版本，确认成片序列输入数量和连线正确。
  8. 刷新页面，确认参考图、分镜图、视频、采用状态全部回填。
- 工程验收：
  - `npm run test` 通过。
  - `npm run build` 通过。
  - 后端相关测试通过。
  - 真实生成失败时页面不白屏，失败节点显示错误原因并可重试。

## Assumptions

- 先做“原型工作台里的真实任务适配层”，不直接接入外部商业生图/视频服务；当前后端可继续用模拟真实任务生成占位资产。
- 不重做旧导演模式和高级节点模式。
- 不引入新数据库表，除非执行阶段发现 `asset_links` 无法可靠承载参考图资产。
- 参考图节点使用现有 `image` 节点类型，通过 `imageRole` 区分用途。
