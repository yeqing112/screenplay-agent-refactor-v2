# 2026-07-27 任务中心重编后继续生成链路验收记录

## 验收目标

- 分镜在执行“重编提示词后继续生成视频 / 首帧”时，前端需要把链路来源一并提交。
- 后端需要持久化该链路元数据，进入创意任务状态与请求快照。
- 任务中心需要可视化展示：
  - 触发链路
  - 重编任务 ID
  - 重编版本
  - 重编原因
  - 本次生成来路说明

## 本次改动

- 后端：
  - `api/server.py`
  - 为 `StoryboardGenerationRequest` 增加链路字段：
    - `generation_chain`
    - `triggered_by_prompt_recompile`
    - `prompt_recompile_reason`
    - `prompt_recompile_task_id`
    - `prompt_recompile_version`
  - 在 `_queue_storyboard_generation_task(...)` 中将上述字段写入任务状态与 `request_payload`

- 前端：
  - `web/src/components/ProductWorkspaceStoryboardSection.tsx`
  - `compileSelectedShotPrompts(...)` 返回重编任务 ID / 版本 / 原因
  - `runPromptRepairAndContinue(...)` 在继续生成时携带链路元数据
  - `runStoryboardGeneration(...)` 支持提交链路元数据

- 任务中心：
  - `web/src/components/productWorkspaceTaskCenterData.ts`
  - `web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx`
  - `web/src/components/productWorkspaceTasks.ts`
  - `web/src/components/productWorkspaceRecovery.ts`
  - 增加“真实任务链路”与“关键输入摘要”中的链路来源展示

## 自动化验证

- `npx vitest run src/components/productWorkspaceTaskCenterData.test.ts src/components/ProductWorkspaceStoryboardSection.test.tsx src/components/ProductWorkspaceStoryboardRepairActions.test.tsx src/components/productWorkspaceStoryboardReferencePayload.test.ts`
  - 结果：通过

- `npm run build`
  - 结果：通过

## 真页面验收

### 本地服务

- 前端：`http://127.0.0.1:5175`
- 后端：`http://127.0.0.1:18765`

### 验收项目

- 项目：`三个和尚`
- 页面：正式产品工作台 -> 任务中心
- 验证对象：视频结果回收任务 `e682e2dd8160`

### 页面可见结果

任务中心详情面板已展示以下信息：

- `触发链路：重编后继续生成视频`
- `重编任务 ID：prompt-7788`
- `重编版本：v12`
- `重编原因：manual-recompile-before-video`
- `生成来路：重编后继续视频`
- `使用重编版本 v12`
- `任务说明：任务 e682e2dd8160 来自“重编后继续生成视频”链路，可在任务中心追溯本次生成所使用的重编任务与提示词版本。`

同时保留了原有输入摘要：

- 首帧输入
- 静态参考图数量
- 服务模式
- 首帧预览
- 静态参考图预览

### 验收结论

- 通过。
- “重编提示词 -> 继续生成视频”的真实任务链路已能在任务中心完成透传、存储与可视化追踪。

## 当前残留

- 目前页面展示的是链路可追踪性，不等于提示词质量问题已经解决。
- 任务中心展示了“重编后继续生成”，但还没有进一步展示：
  - 重编前后提示词差异摘要
  - 本次视频任务实际使用的 prompt version diff
  - 来源于哪一条修复建议/哪一个 QA 问题

## 下一步建议

- 在任务中心补“提示词版本差异摘要”
- 将 QA 修复动作与“重编后继续生成”链路继续打通
- 在镜头工作台补“本次生成将沿用哪一个重编版本”的提交前确认提示
