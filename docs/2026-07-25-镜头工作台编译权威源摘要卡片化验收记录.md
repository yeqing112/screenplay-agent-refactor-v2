# 2026-07-25 镜头工作台编译权威源摘要卡片化验收记录

## 本轮目标

在不丢失编译权威源信息的前提下，继续收敛镜头工作台右侧详情区的信息密度：

1. 将 `编译权威源摘要` 从平铺长列表改为逐项可展开卡片
2. 默认先展示资产类型、资产名称、当前生效版本与短摘要
3. 点开后再查看资产 ID、引用 token、参考来源、参考状态、权威来源等细节
4. 保持现有提示词编译、视觉事实诊断、提示词历史版本等区域不回归

## 本轮实现

### 1. 权威源摘要改为逐项折叠卡片

- 文件：`web/src/components/ProductWorkspaceStoryboardSection.tsx`
- 每个绑定资产改为一个 `details` 卡片
- 折叠态默认展示：
  - 资产类型
  - 资产名称
  - 生效版本标签
  - 权威摘要短预览
- 展开态展示：
  - 资产 ID
  - 引用 token
  - 生效版本
  - 参考来源
  - 参考状态
  - 权威来源

### 2. 自动化回归覆盖

- 文件：`web/src/components/ProductWorkspaceStoryboardSection.test.tsx`
- 补充断言，确认页面中出现 `展开详情`
- 保证卡片化后，既保留原有结构化绑定信息，也保留硬约束与视觉事实区域

## 自动化验证

### 前端测试

```bash
npm --prefix web run test -- ProductWorkspaceStoryboardSection.test.tsx productWorkspacePrompt.test.ts
```

结果：

- `ProductWorkspaceStoryboardSection.test.tsx` 通过
- `productWorkspacePrompt.test.ts` 通过

### 前端构建

```bash
npm --prefix web run build
```

结果：

- 构建通过

## 真页面验收

### 验收环境

- 日期：2026-07-25
- 前端：`http://127.0.0.1:5180`
- 后端：`http://127.0.0.1:18767`
- 前端启动方式：`vite dev`，显式注入 `VITE_API_PROXY_TARGET=http://127.0.0.1:18767`

### 验收路径

1. 打开项目列表页
2. 进入 `三个和尚`
3. 进入 `正式工作台`
4. 切换到 `镜头工作台`
5. 保持 `第 1 集`
6. 查看 `镜头 1`
7. 滚动到 `编译权威源摘要`

### 真实页面结果

页面中可见：

- `编译权威源摘要`
- `绑定资产 5 / 约束 0 / 反馈 0 / 告警 1`
- 每个资产都改为单独卡片
- 每个卡片默认显示短摘要，不再把全部字段一次性平铺出来
- 每个卡片都有 `展开详情`

本次真实页面中已验证到以下卡片示例：

- 场景：`寺庙后院水房`
  - 摘要：`古代寺庙后院水房 / 清晨 / 宏观 / 微仰视`
- 人物：`和尚丙 / 和尚乙 / 和尚甲`
  - 摘要：`人物分集默认定妆设定板 / 展示同一个角色在当前剧集默认状态下的六个视角 / 上排为脸部特写 / 正面、侧面、45度`
- 道具：`木桶`
  - 摘要：`木质水桶 / 圆柱形 / 外壁有多道竹箍或铁箍 / 桶口两侧有对称桶耳`

同时确认以下区域仍然正常存在，没有因卡片化改造丢失：

- `当前修复入口`
- `首轮编译硬约束摘要`
- `查看视觉事实目标`
- `提示词历史版本`
- `参考图预览`

### 验收截图

- [three-monks-authority-cards-2026-07-25.png](D:\Work\Project\screenplay-agent-refactor-v2\artifacts\three-monks-authority-cards-2026-07-25.png)

## 结论

本轮改造已达到预期：

1. `编译权威源摘要` 从平铺长列表收敛为逐项卡片
2. 默认阅读负担明显下降，更适合镜头工作台的日常扫描
3. 细节信息仍可展开追溯，没有牺牲生产所需上下文
4. 自动化测试、构建和真页面验收均已通过

## 下一步建议

继续按蓝图推进以下两项：

1. 在一个允许下游继续生产的项目上，完成一次真实 `重新编译提示词` 按钮闭环验收，补齐“可见 -> 可点 -> 可写回版本”的生产级证据
2. 继续收口 `上游门禁 / QA 修复 / 任务中心 / 导出中心` 的统一状态传导，减少“页面可操作但实际上应阻断”的灰区
