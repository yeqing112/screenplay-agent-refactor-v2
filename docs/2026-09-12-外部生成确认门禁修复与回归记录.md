# 外部生成确认门禁修复与回归记录

日期：2026-09-12

## 本次实施

- `StoryboardGenerationRequest` 增加 `confirmed` 与 `allow_external_call`（兼容 `allowExternalCall`）字段。
- 分镜图片/视频生成请求在进入任务队列前统一经过外部调用确认门禁。
- 未确认的真实 Provider 请求返回 `409 external_call_confirmation_required`，不创建任务、不调用供应商。
- 更新真实 Provider 测试请求，明确携带 `confirmed=true` 与 `allowExternalCall=true`。
- 新增回归用例，验证未确认请求 fail-closed。
- Agent 真浏览器 E2E 改为运行时读取当前样本资产，不再依赖已清理的旧书号、角色名或资产 ID。

## 验证结果

- 定向回归：`51 passed`
- 外部调用门禁用例：`1 passed`
- 生产门禁：后端 `601 passed`、Golden `5/5`、运行时配置检查通过、前端生产构建通过。
- 业务 E2E：通过。
- 机器提示词导出 E2E：通过。
- Agent 真浏览器 E2E：使用 `E2E_AGENT_BOOK_ID=990400` 通过，控制台错误 `0`，覆盖自由对话、项目动态、镜头/资产/QA 承接、过期提案和窄屏抽屉。
- 只读媒体预检：`book 990400 / 第1集 / 镜头1–3` 对 GPT Image 2 与 MiniMax H3 均返回 `ready_for_real_submit=true`；仅保留“提交前发布七牛参考图”的提示，未上传、未调用供应商。
- 灰度预检脚本已改为自动发现当前项目，并以多参考图为默认视频输入（只有显式 `--use-first-frame` 才启用首帧）；当前自动样本 `990301 / 第1集 / 镜头1` 因参考图仍是本地/失效 URL 被正确阻断，未上传、未调用供应商。

## 尚未执行

- GPT Image 2 真实分镜图灰度（需用户显式确认）。
- MiniMax H3 多参考真实视频灰度（需用户显式确认）。
- GitHub 云端 CI 实跑（按计划暂缓）。
