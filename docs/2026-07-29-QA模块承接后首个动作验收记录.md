# 2026-07-29 QA 模块承接后首个动作验收记录

## 本轮目标

- 补齐 `创作画布 Beta -> QA 修复` 的局部承接动作卡
- 让 QA 不只显示“从哪里来”，还明确显示“下一步先做什么”
- 首个动作必须接到真实执行链路，而不是静态说明

## 本轮实现

1. 在 `ProductWorkspaceQaSection` 中新增 `buildQaCanvasPrimaryActionPlan(...)`
2. 根据真实状态给出首个动作优先级：
   - 上游未放行 -> 返回剧本工作台补放行
   - 工单待复检 -> 触发复检
   - 已生成修复方案 -> 自动修复
   - 工单待处理 -> 生成修复方案
   - 其它情况 -> 落到推荐修复入口
3. 在 QA 详情页的创作画布承接卡中新增：
   - `承接后的首个动作`
   - `立即继续`
4. 按钮直接复用真实链路：
   - `runWorkbenchIssueAction('generate' | 'autofix' | 'recheck')`
   - `onNavigate('scripts' | 'storyboard' | 'assets')`
   - `onSelectShot(...)`

## 自动化验收

- 命令：`cd web && npm run test -- ProductWorkspaceQaSection`
- 结果：通过，`5 passed`

- 命令：`cd web && npm run build`
- 结果：通过

## 真浏览器验收

- 入口：`http://127.0.0.1:5175`
- 路径：`三个和尚 -> 创作画布 Beta -> 第 1 集质检 -> 打开对应模块`
- 验收结果：
  - 成功进入 QA 模块
  - 页面存在 `承接后的首个动作`
  - 页面存在 `立即继续`
  - QA 页面与创作画布承接链路保持连贯

## 产出物

- 代码：
  - [ProductWorkspaceQaSection.tsx](D:\Work\Project\screenplay-agent-refactor-v2\web\src\components\ProductWorkspaceQaSection.tsx)
  - [ProductWorkspaceQaSection.test.tsx](D:\Work\Project\screenplay-agent-refactor-v2\web\src\components\ProductWorkspaceQaSection.test.tsx)
- 截图：
  - [qa-canvas-handoff-acceptance.png](D:\Work\Project\screenplay-agent-refactor-v2\.codex-artifacts\qa-canvas-handoff-acceptance.png)

## 结论

本轮验收通过。  
创作画布到 QA 的链路已经从“能跳过去”提升为“跳过去后知道先做什么，并且可以立刻做”。
