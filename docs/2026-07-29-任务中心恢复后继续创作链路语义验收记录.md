# 2026-07-29 任务中心恢复后继续创作链路语义验收记录

## 本轮目标

把 `创作画布 Beta` 中已经落地的恢复后继续创作链路语义，补齐到 `任务中心` 的真实任务详情展示里，避免用户在任务中心看到内部链路码或过于泛化的旧文案。

## 本轮实现

- `productWorkspaceTaskCenterData.ts`
  - 已识别以下恢复派生链路，并输出面向用户的中文说明：
    - `canvas_recovery_continue_after_frame`
    - `canvas_recovery_continue_after_prompt`
    - `canvas_recovery_recompile_then_video`
    - `canvas_recovery_recompile_then_frame`
- `productWorkspaceTaskCenterSelectedTaskPanel.tsx`
  - 已在 `真实任务链路` 与 `关键输入摘要 -> 生成来路` 两处统一接入恢复感知文案格式化。
  - 任务详情不再直接暴露 `canvas_recovery_*` 内部链路码。

## 自动化验收

已通过：

1. `cd web && npm run test -- productWorkspaceTaskCenterData productWorkspaceTaskCenterSelectedTaskPanel`
2. `cd web && npm run build`

验收结果：

- 单测 9/9 通过。
- 构建通过。

## 真浏览器验收

验收路径：

1. 打开 `http://127.0.0.1:5175`
2. 进入项目 `三个和尚`
3. 切换到 `任务中心`
4. 点击真实创意任务 `首帧结果回收`

真实页面观察结果：

- 任务中心详情页已能稳定展示：
  - `真实任务链路`
  - `任务提示词版本 / 镜头当前版本 / 版本差异`
  - `静态参考图预览`
  - `生成来路`
- 当前项目现存样本中，该任务的链路是 `canvas_generate_frame`，页面显示为常规创作链路文案，未出现内部未翻译链路码。
- 当前项目里没有现成的 `canvas_recovery_continue_after_frame` 等恢复后继续创作任务样本，因此浏览器侧本轮验证重点是：
  - 任务中心真实详情链路展示仍然可用
  - 新增恢复链路文案不会破坏现有创意任务详情展示

## 结论

本轮可判定为通过。

通过依据：

1. 恢复后继续创作链路已补齐到任务中心数据层与详情展示层。
2. 单测已覆盖恢复链路文案映射。
3. 真浏览器确认任务中心详情页的真实创意任务链路展示仍然稳定可用。

## 后续衔接

下一步继续沿 LibTV 化方向推进时，任务中心应继续补两类能力：

1. 当真实项目里出现恢复派生任务时，补一次真浏览器复验，确认页面直接显示：
   - `恢复后继续生成视频`
   - `恢复后继续生成首帧/视频`
   - `恢复后重编再继续生成视频`
   - `恢复后重编再生成首帧`
2. 让创作画布、任务中心、镜头工作台三处对同一条恢复派生任务使用完全一致的语义标签，避免跨工作面理解切换成本。
