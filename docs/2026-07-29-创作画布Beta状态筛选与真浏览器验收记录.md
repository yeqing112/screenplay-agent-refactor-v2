# 2026-07-29 创作画布 Beta 状态筛选与真浏览器验收记录

## 本轮目标

继续按《产品重构蓝图》和《LibTV化融合重构计划》推进 `创作画布 Beta`，补齐从“能看图谱”到“能用图谱定位问题”的关键一步：

1. 节点状态不再只靠用户猜。
2. 画布里可以直接筛出阻塞节点。
3. 画布里可以直接筛出缺参考图节点。
4. 真浏览器链路保持可用，不因新增筛选破坏原有流转。

## 本轮实现

### 1. 图谱节点补充状态语义

在 [web/src/components/productWorkspaceCanvasBeta.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceCanvasBeta.ts) 中，为节点补充：

- `hasOutput`
- `hasBlocker`
- `missingReference`

并基于真实项目数据推导：

- 剧本节点：有无剧本文本
- 资产节点：是否已有参考图、是否缺参考图、是否存在陈旧引用风险
- 分镜节点：是否已有图片/视频产出、是否缺静态/运动提示词、是否缺参考图、是否存在编译阻塞
- QA 节点：是否存在 QA 问题
- 交付节点：是否达到基本可交付状态

### 2. 画布界面新增状态筛选

在 [web/src/components/ProductWorkspaceCanvasBetaSection.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspaceCanvasBetaSection.tsx) 中新增：

- `全部状态`
- `阻塞`
- `有产出`
- `缺参考图`

同时补充：

- 节点状态标签
- 结果侧栏状态说明
- 当前状态筛选可视反馈

### 3. 清理画布中文文案乱码

本轮顺手把画布主文件中的中文文案恢复为正常中文，避免验收与后续维护时继续被乱码干扰判断。

## 构建与测试

本轮已执行：

1. `npm run test -- productWorkspaceCanvasBeta`
2. `npm run build`

结果：

- 单测通过
- 前端构建通过

并新增测试覆盖：

- 有产出节点标记
- 阻塞节点标记
- 缺参考图节点标记

测试文件：

- [web/src/components/productWorkspaceCanvasBeta.test.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceCanvasBeta.test.ts)

## 真浏览器验收

验收日期：2026-07-29

验收路径：

`首页 -> 三个和尚 -> 正式工作台 -> 创作画布 Beta`

验收结果：

1. 页面可正常进入 `创作画布 Beta`
2. 搜索 `镜头 8` 后，配合 `分镜` 类型筛选，结果可收敛为 1 个节点
3. 右侧 `节点详情` 与 `打开对应模块` 保持可见
4. 点击 `阻塞` 状态筛选后，页面能显示真实阻塞节点
5. 点击 `缺参考图` 状态筛选后，页面能显示真实缺参考图节点
6. 清空状态后，搜索与类型筛选仍可正常工作

验收采样结果：

- `阻塞` 筛选结果数：27
- `缺参考图` 筛选结果数：3
- `镜头 8 + 分镜` 聚焦结果数：1

说明：

- `当前结果` 在全量画布下显示的是当前可见节点数量，而不是原始全量对象总数，这一行为目前符合当前画布定义。

## 对照蓝图的结论

本轮满足了以下蓝图要求：

1. 创作画布不再是静态展示页，而开始具备问题定位价值。
2. 用户可以在不进入各个模块之前，先从全局视角发现阻塞和缺口。
3. 真实浏览器链路保持成立，没有因增量改动破坏主流程。

## 当前仍未完成

本轮还没有完成以下项：

1. 从 `任务中心 / 资产中心 / 镜头工作台` 定向进入画布并自动聚焦节点
2. 节点分组、折叠、层级聚合
3. 画布上的可执行节点闭环
4. 任务中心与画布节点的深度双向恢复链路

## 下一步建议

按优先级建议继续推进：

1. 先补“带上下文进入画布”的回跳能力
2. 再做节点分组/折叠，降低真实项目节点规模继续增大后的浏览成本
3. 然后进入 L2：最小可执行节点闭环
