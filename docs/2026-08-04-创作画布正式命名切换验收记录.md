# 2026-08-04 创作画布正式命名切换验收记录

## 1. 背景

根据蓝图与 LibTV 化融合计划，创作画布已经不再只是一个只读实验页，而是正式工作台里的高频创作入口之一。

但当前正式工作台用户可见口径仍显示 `创作画布 Beta`，这会持续向用户传递“附属试验入口”的信号，不利于后续把画布升级为正式主入口。

本次目标是：

- 在不大规模改动内部文件名与历史文档的前提下
- 先把正式工作台里的用户可见命名切换为正式 `创作画布`

## 2. 本次改动

更新文件：

- [ProductWorkspace.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspace.tsx)
  - 正式工作台一级页签 `canvas` 的展示文案从 `创作画布 Beta` 调整为 `创作画布`
- [ProductWorkspaceCanvasBetaSection.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspaceCanvasBetaSection.tsx)
  - 空态标题从 `创作画布 Beta` 调整为 `创作画布`
  - 主视图标题从 `创作画布 Beta` 调整为 `创作画布`

说明：

- 本轮只调整正式工作台中的用户可见命名
- 内部文件名、测试文件名和历史验收记录仍保留 `CanvasBeta` / `创作画布 Beta`，避免一次性掀动过大范围

## 3. 自动化验证

### 3.1 测试

执行：

```bash
npm run test -- ProductWorkspaceCanvasBetaSection productWorkspaceCanvasBeta
```

结果：

- 2 个测试文件全部通过
- 22 个测试全部通过

### 3.2 构建

执行：

```bash
npm run build
```

结果：

- 前端全量构建通过

## 4. 真浏览器验收

验收环境：

- 前端：`http://127.0.0.1:5175`
- 后端：`http://127.0.0.1:18765`
- 验收项目：`book 14 / 三个和尚`

验收步骤：

1. 打开首页
2. 进入 `三个和尚 / 正式产品工作台`
3. 检查一级页签命名
4. 点击进入 `创作画布`
5. 检查页内标题与基础控件

验收结果：

- 正式工作台一级页签已显示 `创作画布`
- 正式工作台一级页签中不再出现 `创作画布 Beta`
- 进入画布后页内标题显示 `创作画布`
- 页内仍可正常看到：
  - `搜索节点`
  - `当前分集`
- 真浏览器确认：
  - `workspaceHasCanvasLabel = true`
  - `workspaceHasOldBetaLabel = false`
  - `canvasPageHasTitle = true`
  - `canvasPageHasOldBetaLabel = false`

相关截图：

- `/.codex-artifacts/browser-20260804-canvas-tab-label.png`
- `/.codex-artifacts/browser-20260804-canvas-page-label.png`

## 5. 结论

本次创作画布正式命名切换验收通过。

这一步的意义不只是文字调整，而是把正式工作台对画布的产品信号，从“并行实验入口”继续推进到“正式创作入口”。
