# 2026-07-29 任务中心到创作画布恢复链路验收记录

## 本轮目标

继续按 LibTV 化融合计划推进 `创作画布 Beta`，补上 `任务中心 -> 创作画布` 的真实恢复入口，让画布开始承接生产任务的上下文回跳。

## 本轮实现

### 1. 任务详情新增创作画布入口

在 [productWorkspaceTaskCenterSelectedTaskPanel.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx) 中：

- 对带有 `episode / shotId / assetId` 的任务显示 `在创作画布查看`
- 点击后保留：
  - 集别
  - 镜头
  - 资产
  - 任务 ID
  - 恢复类型
  - 恢复意图

### 2. 画布上下文链路复用既有导航目标

本轮没有新起一套状态，而是复用了上一轮已经接好的 `canvasNavigationTarget`，保证：

- 任务中心
- 镜头工作台
- 资产中心

都通过同一套画布上下文入口进入，避免后续出现三套互不兼容的跳转规则。

### 3. 新增测试覆盖

新增测试文件：

- [productWorkspaceTaskCenterSelectedTaskPanel.test.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.test.tsx)

覆盖点：

- 带明确上下文的任务详情会渲染 `在创作画布查看`

## 构建与测试

本轮已执行：

1. `npm run test -- productWorkspaceCanvasBeta productWorkspaceTaskCenterSelectedTaskPanel`
2. `npm run build`

结果：

- 测试通过
- 构建通过

## 真浏览器验收

验收日期：2026-07-29

验收路径：

`三个和尚 -> 正式工作台 -> 任务中心 -> 资产补全 -> 在创作画布查看`

验收结果：

1. 任务详情中可见 `在创作画布查看`
2. 点击后成功进入 `创作画布 Beta`
3. 页面保持第 1 集上下文
4. 页面中仍可见镜头 / 人物 / 场景 / 道具等真实节点语义
5. 右侧 `节点详情` 与 `打开对应模块` 仍可见

## 验收结论

本轮说明：

1. 创作画布已经接入第三条正式入口，不再只是独立浏览页。
2. 任务中心里的问题定位，开始可以直接回到全局依赖图继续追踪。
3. 这让创作画布更接近“恢复面 / 控制面”，而不只是“关系图”。

## 下一步

按当前计划，下一步建议继续推进：

1. 节点分组 / 折叠，降低大项目浏览成本
2. L2 最小可执行节点闭环
   - 提示词编译节点
   - 单镜头生图节点
   - 结果回写镜头工作台
