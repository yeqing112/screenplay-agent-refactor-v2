# 2026-08-03 首页项目卡片状态文案清理验收记录

## 1. 背景

在正式工作台、内容准备页、资产中心、导出中心等高频页面完成多轮文案清理后，首页项目列表仍残留 `Bible 完成` 文案，导致真实用户从首页进入时仍会感知到旧术语和不统一状态口径。

本次验收目标是将首页项目卡片状态文案与正式工作台统一为中文产品语义，并确认真浏览器下不再出现该遗留词。

## 2. 本次修复

### 2.1 代码调整

- 更新 [ProjectsPage.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/pages/ProjectsPage.tsx)
  - 将 `bibled` / `bibeled` 的首页显示文案从 `Bible 完成` 统一调整为 `小说圣经完成`
  - 导出 `resolveProjectStatusLabel`，便于后续对首页项目状态映射做单测约束
- 更新 [ProjectsPage.test.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/pages/ProjectsPage.test.ts)
  - 新增 `bibled` / `bibeled` 中文映射断言

## 3. 自动化验证

### 3.1 测试

执行：

```bash
npm run test -- ProjectsPage
```

结果：

- `src/pages/ProjectsPage.test.ts` 3 个测试全部通过

### 3.2 构建

执行：

```bash
npm run build
```

结果：

- 前端构建通过

## 4. 真浏览器验收

验收环境：

- 前端：`http://127.0.0.1:5175`
- 后端：`http://127.0.0.1:18765`

验收步骤：

1. 打开首页项目列表
2. 观察项目卡片状态标签
3. 进入 `三个和尚 / book 14` 的正式工作台
4. 复验 `任务中心` 与 `导出中心`
5. 返回项目列表再次检查首页状态标签

验收结果：

- 首页项目列表可正常打开
- 项目卡片不再出现 `Bible 完成`
- 首页已出现统一后的 `小说圣经完成`
- `任务中心` 页面可正常进入，且未出现历史乱码 token
- `导出中心` 页面可正常进入，`交付 readiness`、`交付包内容` 等关键区域显示正常
- 返回首页后再次确认：
  - `hasLegacyBibleLabel = false`
  - `hasCleanStatus = true`
  - `badTokens = []`

相关截图：

- `/.codex-artifacts/browser-task-center-20260803.png`
- `/.codex-artifacts/browser-delivery-20260803.png`
- `/.codex-artifacts/browser-project-list-status-clean-20260803.png`

## 5. 结论

本次首页项目卡片状态文案清理已通过验收。

本轮之后，首页入口与正式产品工作台在高频状态词上已经统一，不再存在用户从首页进入就看到 `Bible 完成` 的割裂感。
