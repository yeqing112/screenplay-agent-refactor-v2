# 2026-07-20 项目入口与资产中心验收记录

## 本轮修复

- 修复 `web/src/pages/ProjectsPage.tsx` 的项目列表页可见乱码文案。
- 修复 `web/src/pages/projectDisplayText.ts` 的项目标题降级与损坏标题识别逻辑文案。
- 修复 `web/src/components/ProductionMode.tsx` 中历史交付记录状态与统计字段的可见乱码。
- 重新校正本地联调环境，使前端实际指向 `http://127.0.0.1:18765`。

## 自动化验证

- `npm run build`
- `npm test -- ProductWorkspaceAssetsSection.test.tsx`

## 真浏览器验收

- 日期：2026-07-20
- 环境：
  - 前端：`http://127.0.0.1:5175/`
  - 后端：`http://127.0.0.1:18765/`
- 路径：
  - `项目列表`
  - `三个和尚 -> 正式工作台 -> 资产中心`

## 浏览器结果

### 项目列表页

- 页头显示 `API Proxy: http://127.0.0.1:18765`
- `产品原型 Demo`、`模型管理`、`全部项目`、`新建项目` 文案正常
- 项目卡片状态文案正常，如 `Bible 完成`、`人物设定完成`
- 未发现新的 `???` 型乱码

### 资产中心

- `资产列表`、`参考图管理`、`资产影响范围` 三个核心区域可见
- `预览`、`删除`、`查看来源`、`重生新版参考图` 等关键动作入口可见
- 右侧详情区已正常渲染，不再出现前一轮的编译破损
- 资产中心列表、筛选、详情三栏可在同一页面稳定显示

## 结论

- 2026-07-20 这轮本地联调下，项目入口页与正式工作台资产中心已恢复可用。
- 当前主要剩余问题已经从“页面打不开/文案大面积损坏”收敛为“数据完整性、提示词质量、后续链路闭环”。
