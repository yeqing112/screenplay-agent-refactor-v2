# 2026-07-29 创作画布 Beta 分组折叠与任务回跳验收记录

## 验收范围

- 创作画布 Beta 新增按资产类型折叠分组能力
- 状态筛选与分组折叠组合使用
- 任务中心进入创作画布的真实上下文回跳

## 本次实现

1. 在 `ProductWorkspaceCanvasBetaSection.tsx` 中新增：
   - `折叠人物`
   - `折叠场景`
   - `折叠道具`
   - `展开全部分组`
2. 新增 `collapseCanvasGraphByKind(...)`，用于把同集同类资产节点折叠为组节点，并聚合：
   - `hasOutput`
   - `hasBlocker`
   - `missingReference`
3. 选中逻辑补强：
   - 当导航目标节点已被折叠时，自动落到对应分组节点
4. 补充单测：
   - `productWorkspaceCanvasBeta.test.ts`
   - 验证分组节点生成、状态聚合、边重写与去重

## 构建与测试

- `npm run build` 通过
- `npm run test -- productWorkspaceCanvasBeta productWorkspaceTaskCenterSelectedTaskPanel` 通过

## 真浏览器验收

验收地址：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)

验收项目：

1. 进入 `三个和尚 -> 正式工作台 -> 创作画布 Beta`
2. 点击 `阻塞 27`
3. 点击 `折叠人物`
4. 点击 `展开全部分组`
5. 进入 `任务中心`
6. 点击 `在创作画布查看`

验收结果：

- `阻塞` 状态筛选生效
- `折叠人物` 生效，页面出现 `已折叠人物`
- 折叠后仍可查看筛选结果与节点详情
- `展开全部分组` 可恢复默认状态
- 任务中心存在 `在创作画布查看`
- 从任务中心返回后成功进入 `创作画布 Beta`
- 回跳后页面仍保留可读的筛选结果与上下文说明

## 当前结论

本轮创作画布 Beta 已从“可看”继续推进到“可折叠、可筛选、可从任务恢复回跳”的阶段，L1 可继续向 L2 最小可执行节点闭环推进。
