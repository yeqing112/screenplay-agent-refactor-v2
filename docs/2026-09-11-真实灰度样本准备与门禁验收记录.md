# 真实 LLM 灰度样本准备与门禁验收记录

## 目的

为 Director Runtime 的第二次真实 `mimo-v2.5` clone-only 灰度准备一个证据完整、可复现且不触碰生产项目的样本。样本创建、编译、回滚和清理均通过通用 ORM/API 路径完成，不包含书号、角色名或镜头号特例逻辑。

## 样本

- 项目：`导演运行时灰度样本`（当前本地 ID `990301`，可通过脚本参数替换）
- 集数/镜头：第 1 集 / 镜头 1–3（共享同一场景和人物资产，动态动作各不相同）
- 场景资产：`雨夜旧公寓门厅`，结构化字段完整，参考图状态 `locked`
- 人物资产：`林晚`，服装/发型/妆容/身份字段完整，参考图状态 `locked`
- 镜头证据：导演动作、开始状态、结束状态、持续时长、机位/运动、2 个结构化 action beats
- 关键约束：禁止木门、禁止白天、禁止新增人物；镜头动作保持铁门连续

## 可复现命令

```powershell
python scripts/seed-storyboard-gray-sample.py [book_id]
$env:STORYBOARD_REAL_LLM_GRAY_MOCK = "1"
$env:STORYBOARD_REAL_LLM_GRAY_TARGETS = "990301:1:1,990301:1:2,990301:1:3"
$env:STORYBOARD_REAL_LLM_GRAY_SHOT_COUNT = "3"
python scripts/validate-storyboard-real-llm-gray.py
```

脚本只清理传入项目 ID 的样本表，不执行全局删除。灰度验证仍在临时项目 `999905` 上编译，结束后回滚并清理。

## 本地门禁结果

- deterministic mock：`3/3 passed`
- after-audit：`0 errors / 0 warnings`
- 结构摘要：每个镜头均绑定场景 1、人物 1、动作节拍 2
- 真实供应商调用：`0`（本轮仅验证样本和安全路径）
- 批量报告：`artifacts/storyboard-real-llm-gray-20260911T001326Z.json` / `.md`
- 专项回归：Prompt Compiler / Decision Evidence / Director Benchmark 共 `41 passed`

## 真实调用命令（需显式确认）

只有在用户明确确认后，才允许执行以下命令进行一次真实、可计费的 MiMo clone-only 灰度：

```powershell
$env:STORYBOARD_REAL_LLM_GRAY_RUN = "1"
$env:STORYBOARD_REAL_LLM_GRAY_USE_REAL_LLM = "1"
$env:STORYBOARD_REAL_LLM_GRAY_TARGETS = "990301:1:1,990301:1:2,990301:1:3"
$env:STORYBOARD_REAL_LLM_GRAY_SHOT_COUNT = "3"
python scripts/validate-storyboard-real-llm-gray.py
```

该命令不创建正式 Prompt Version、不提交图片/视频、不修改源项目；报告记录非敏感 token、缓存命中率、延迟和审计结果。

## 真实灰度结果（2026-09-11）

- 已经用户明确确认后完成 `990301 / 第1集 / 镜头1` 的一次真实 `mimo-v2.5` 调用。
- 结果：`1/1 passed`；after-audit `0 errors / 0 warnings`；compiler diagnostics `pass`。
- 供应商调用：1 次；输入 28,841 tokens，输出 587 tokens，总计 29,428 tokens；缓存命中 0（首次唯一请求）；延迟约 16,046 ms。
- 真实调用只发生在临时克隆 `999905`；随后已回滚并清理，源项目无写入，未创建正式 Prompt Version、图片或视频任务。
- 报告：`artifacts/storyboard-real-llm-gray-20260911T000429Z.json` / `.md`。
- 若要测量前缀缓存收益，需要对相同证据包再次发起一次独立且明确确认的真实调用；本轮不自动重试或重复计费。

## 缓存复测结果（2026-09-11）

- 已经用户再次明确确认，对完全相同证据包执行第二次真实调用。
- 结果：`1/1 passed`；after-audit `0 errors / 0 warnings`；compiler diagnostics `pass`。
- 供应商返回输入 `28,841` tokens，其中缓存 `28,800`，缓存命中率 `99.8578%`；输出 `638`，总计 `29,479` tokens；延迟约 `11,712 ms`。
- 说明：该结果证明当前稳定前缀组织对 MiMo 生效，但仅代表本样本/当前供应商窗口，不应直接当作 SLA。后续模型或证据版本变化仍需重新观测。
- 报告：`artifacts/storyboard-real-llm-gray-20260911T000856Z.json` / `.md`。

## 多样本缓存观察结果（2026-09-11）

- 经用户显式确认，对共享同一场景/人物稳定资产、但动态任务不同的镜头 `1–3` 执行真实调用。
- 结果：`3/3 passed`；after-audit `0 errors / 0 warnings`；compiler diagnostics 均为 `pass`；无 fallback。
- 总输入 `87,052` tokens，供应商返回缓存 `12,288` tokens，聚合命中率 `14.1157%`；平均耗时 `16.876s`，最大 `18.461s`；输出 `1,777` tokens。
- 每个不同动态后缀请求均命中约 `4,096` tokens，说明当前供应商窗口只复用了有限公共前缀；此前“同一请求重复”达到 `99.8578%`，不能外推到不同镜头。
- 结论：当前请求组织没有质量回归且缓存遥测真实可观测；跨镜头缓存收益有限，暂不引入未经供应商文档确认的私有缓存字段，也不自动改变模型或思考开关。
- 报告：`artifacts/storyboard-real-llm-gray-20260911T001706Z.json` / `.md`。
