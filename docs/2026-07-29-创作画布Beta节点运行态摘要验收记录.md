# 2026-07-29 创作画布 Beta 节点运行态摘要验收记录

## 本轮目标

验证 `创作画布 Beta` 中镜头节点是否已经具备最小可用的运行态可见性：

1. 执行后可回写 `最近执行`
2. 长任务未完成时可显示 `待回收`
3. `重编后继续生成视频` 链路的请求参数仍然正确

## 本轮改动

涉及文件：

- `web/src/components/productWorkspaceRecovery.ts`
- `web/src/components/productWorkspaceCanvasBeta.ts`
- `web/src/components/ProductWorkspaceCanvasBetaSection.tsx`
- `web/src/components/productWorkspaceTaskCenterListPanel.tsx`
- `web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx`
- `web/src/components/ProductWorkspaceTasksSection.tsx`
- `web/src/components/ProductWorkspaceStoryboardSection.tsx`

新增能力：

1. 本地持久化镜头执行摘要 `ShotExecutionSummary`
2. 画布镜头节点追加运行态元信息：
   - 已采纳首帧
   - 已采纳视频
   - 最近执行
   - 待回收
3. 创作画布单镜头动作成功后写回执行摘要：
   - `提示词重编译 vN`
   - `生成首帧`
   - `重编后生成首帧`
   - `生成视频`
   - `重编后继续生成视频`
4. 画布节点卡片直接显示运行态徽标：
   - `最近执行`
   - `待回收 N`
   - `已采纳首帧`
   - `已采纳视频`
5. 任务中心详情页新增 `当前镜头运行态` 摘要卡，与画布读取同一份本地恢复数据。
6. 任务中心列表直接显示镜头级运行态标签：
   - `最近执行 · ...`
   - `待回收 · ...`
7. 镜头工作台新增 `当前镜头运行态` 卡片，并在镜头页写回本地执行摘要。

## 构建验收

执行：

```bash
cd web
npm run build
```

结果：

- 构建通过
- `ProductWorkspaceCanvasBetaSection.tsx` 编码污染已修复
- 画布动作区与右侧详情区恢复正常编译
- 任务中心新增运行态摘要后仍能通过类型检查

补充测试：

```bash
cd web
npm run test -- productWorkspaceCanvasBeta productWorkspaceTaskCenterSelectedTaskPanel ProductWorkspaceStoryboardSection
```

结果：

- 3 个测试文件通过
- 16 个测试全部通过

## 真浏览器验收

验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765/api/books](http://127.0.0.1:18765/api/books)
- 项目：`三个和尚`
- 页面路径：`正式工作台 -> 创作画布 Beta`

验收方式：

- 使用 Playwright 真浏览器操作
- 拦截编译/生视频接口，避免真实付费调用

### Case 1：重编后继续生成视频，验证待回收

拦截结果：

- `POST /compile-prompts/async`
- `GET /api/storyboard-prompt-compile-tasks/canvas-prompt-rt-001`
- `POST /generate-video`

校验点：

1. 编译请求体包含：
   - `compileReason: "manual-recompile-before-video"`
   - `force: false`
2. 视频请求体包含：
   - `compileIfMissing: true`
   - `generationChain: "recompile_then_video"`
   - `triggeredByPromptRecompile: true`
   - `promptRecompileTaskId`
   - `promptRecompileVersion`
   - `referenceAssetIds`
   - `firstFrameAssetId`
3. 右侧节点详情出现：
   - `最近执行：提示词重编译 v21`
   - `待回收：视频`
   - `待回收视频任务：canvas-video-rt-001`

结论：

- 通过

### Case 2：重编后继续生成视频，验证成功回写

拦截结果：

- `POST /compile-prompts/async`
- `GET /api/storyboard-prompt-compile-tasks/canvas-prompt-rt-002`
- `POST /generate-video`
- `GET /api/prototyping/tasks/canvas-video-rt-002`

校验点：

1. 右侧节点详情出现：
   - `最近执行：重编后继续生成视频`
   - `视频已生成并写回镜头。`
   - `已重新编译到 v22。`
2. 任务成功后不再显示 `待回收：视频`

结论：

- 通过

### Case 3：画布节点卡片显示运行态徽标

验证方式：

- 在浏览器中为 `第 1 集 / 镜头 3` 注入本地运行态样本
- 返回 `创作画布 Beta` 检查节点文本

校验点：

1. 镜头节点卡片出现：
   - `最近执行`
   - `待回收 1`
   - `已采纳首帧`
2. 节点仍保留原有镜头摘要信息，不影响筛选和阅读

结论：

- 通过

### Case 4：任务中心显示当前镜头运行态摘要

验证方式：

- 在浏览器中为 `第 1 集 / 镜头 3` 注入本地执行摘要与待回收任务
- 进入 `任务中心`
- 选中 `首帧结果回收`

校验点：

1. 详情页出现 `当前镜头运行态`
2. 摘要卡内能看到：
   - `最近执行：生成首帧`
   - `待回收任务：1`
   - `运行态建议：先回收任务`
3. 该摘要与画布读取同一份本地恢复数据

结论：

- 通过

### Case 5：任务中心列表可直接扫描运行态

验证方式：

- 在浏览器中为 `第 1 集 / 镜头 3` 注入本地执行摘要与待回收任务
- 进入 `任务中心`

校验点：

1. `首帧结果回收` 任务卡直接出现：
   - `最近执行 · 生成首帧`
   - `待回收 · 视频`
2. 用户不进入右侧详情也能判断该镜头还有后续恢复任务

结论：

- 通过

### Case 6：镜头工作台显示当前镜头运行态

验证方式：

- 从正式工作台进入 `镜头工作台`
- 选中带运行态记录的镜头

校验点：

1. 镜头页出现 `当前镜头运行态`
2. 卡片内能看到：
   - `最近执行：重编后继续生成视频`
   - `待回收任务：无`
   - `建议动作：继续复核结果`
3. 运行态卡与视频输入摘要、编译诊断并存，不影响原有镜头页结构

结论：

- 通过

## 当前结论

`创作画布 Beta` 已从“只能发起动作”推进到“能看见当前镜头最近做了什么、是否还有任务待回收”。

当前这条链路已经满足：

1. 画布内发起真实创意任务
2. 任务中心与画布共享恢复语义
3. 成功与未完成两种运行态都能在节点侧回显
4. 任务中心能读到同源镜头运行态摘要，不再只展示后端任务元数据
5. 任务中心列表已经具备可扫描的镜头级运行态标签
6. 镜头工作台已接入同源执行摘要，形成画布 / 任务中心 / 镜头工作台三端一致

## 后续建议

下一步建议继续补三件事：

1. 给 `待回收` 增加更新时间与任务来源说明
2. 让任务中心列表支持按 `待回收 / 最近执行 / 已采纳状态` 做轻量筛选
3. 把同一套运行态继续前推到资产中心，打通“资产生成任务 -> 镜头引用 -> 回收状态”的可见性

## 2026-07-29 补充验收：资产中心运行态接入

本轮新增验收范围：

1. `资产中心` 资产卡片直接显示镜头级运行态摘要
2. 选中资产后，详情面板显示 `关联镜头运行态`
3. 详情面板可读出：
   - 最近执行动作
   - 对应镜头
   - 待回收任务数量
   - 任务类型
   - 更新时间
   - 任务 ID

涉及文件：

- `web/src/components/ProductWorkspaceAssetsSection.tsx`
- `web/src/components/ProductWorkspaceAssetsSection.test.tsx`
- `web/src/components/productWorkspaceSectionContracts.ts`
- `web/src/components/ProductWorkspaceSectionContent.tsx`

构建与测试：

```bash
cd web
npm run build
npm run test -- ProductWorkspaceAssetsSection productWorkspaceTaskCenterSelectedTaskPanel ProductWorkspaceStoryboardSection productWorkspaceCanvasBeta
```

结果：

- build 通过
- 4 个测试文件通过
- 29 个测试全部通过

真浏览器验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765/api/books](http://127.0.0.1:18765/api/books)
- 项目：`三个和尚`

验收方式：

- 在浏览器中为 `bookId=14 / 第 1 集 / 镜头 3` 注入本地运行态：
  - `最近执行 = 生成首帧`
  - `待回收 = 视频`
- 进入 `资产中心`
- 检查列表卡片与资产详情

浏览器摘录：

```text
关联镜头运行态
更新于 2026/7/30 02:35:00
最近执行
生成首帧
第 1 集 / 镜头 3
待回收任务
1
第 1 集 / 镜头 3 · 视频
建议动作
先回收任务
任务 ID：runtime-verify-pending-001
```

结论：

- 通过
- `资产中心` 已接入与 `创作画布 Beta / 任务中心 / 镜头工作台` 同源的镜头运行态语义
- 用户现在可以从资产视角直接看到“该资产关联镜头最近做了什么、还有什么待回收”

## 2026-07-29 补充验收：资产中心 -> 任务中心恢复入口

本轮新增目标：

1. 资产详情里的运行态不只展示信息，还能直接继续恢复流程
2. 从 `资产中心` 点击后，能够带着恢复上下文进入 `任务中心`
3. 即使原始任务卡尚未在列表中精确命中，任务中心也要明确说明当前恢复上下文

涉及文件：

- `web/src/components/ProductWorkspaceAssetsSection.tsx`
- `web/src/components/ProductWorkspaceTasksSection.tsx`
- `web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx`
- `web/src/components/productWorkspaceAssetViewController.ts`
- `web/src/components/productWorkspaceSectionContracts.ts`
- `web/src/components/productWorkspaceSectionBundlesController.ts`
- `web/src/components/ProductWorkspaceSectionContent.tsx`

测试：

```bash
cd web
npm run test -- ProductWorkspaceAssetsSection productWorkspaceTaskCenterSelectedTaskPanel
npm run build
```

结果：

- 2 个测试文件通过
- 16 个测试全部通过
- build 通过

真浏览器验收路径：

- `三个和尚 -> 正式产品工作台 -> 资产中心 -> 查看资产 -> 去任务中心继续回收`

验收结果：

1. 资产详情中出现新的恢复动作按钮：
   - `去任务中心继续回收`
   - `去镜头工作台定位镜头`
   - 单条待回收任务旁 `继续回收`
2. 点击 `去任务中心继续回收` 后，页面正确切换到 `任务中心`
3. 任务中心详情面板顶部出现恢复上下文提示：

```text
恢复上下文
第 1 集 / 镜头 3 / 视频回收
当前正在尝试定位任务 runtime-verify-pending-001；如果列表中暂未命中原任务，请优先查看同镜头恢复任务。
```

4. 同一页继续显示：
   - `任务 ID：runtime-verify-pending-001`
   - `当前镜头运行态`
   - `最近执行 -> 生成首帧`
   - `待回收任务 -> 1`

备注：

- 浏览器里显示的 `2026/7/30 02:35:00` 与 `2026/7/30 02:35:05` 来自本次验收注入的 UTC 时间 `2026-07-29T18:35:00.000Z` 与 `2026-07-29T18:35:05.000Z`，被本地时区转换后跨到了 2026 年 7 月 30 日，这属于预期表现。

结论：

- 通过
- `资产中心 -> 任务中心` 已具备“带恢复上下文进入”的真实闭环
- 当前系统不再只是展示恢复状态，而是开始具备 LibTV 式的跨工作面恢复调度体验

## 2026-07-29 补充复验：创作画布 -> 任务中心恢复上下文

复验目标：

1. 确认 `创作画布 Beta` 中原有的“继续回收”按钮已自动接入新的任务中心恢复上下文能力
2. 确认不是只有 `资产中心 -> 任务中心` 生效，而是跨工作面统一生效

真浏览器路径：

- `三个和尚 -> 创作画布 Beta -> 镜头 3 -> 去任务中心继续回收视频`

复验结果：

1. 画布侧可见按钮：
   - `去任务中心继续回收视频`
2. 点击后正确切换到 `任务中心`
3. 顶部恢复上下文正确显示：

```text
恢复上下文
第 1 集 / 镜头 3 / 视频回收
当前正在尝试定位任务 runtime-verify-pending-001；如果列表中暂未命中原任务，请优先查看同镜头恢复任务。
```

4. 同页继续显示：
   - `任务 ID：runtime-verify-pending-001`
   - `恢复类型：视频`
   - `当前镜头运行态`
   - `最近执行 -> 生成首帧`
   - `待回收任务 -> 1`

结论：

- 通过
- `创作画布 Beta -> 任务中心` 已与 `资产中心 -> 任务中心` 使用同一套恢复上下文语义
- 这说明当前恢复链路已经开始从“单模块功能”进化为“跨工作面统一调度能力”

## 2026-07-29 补充复验：普通导航不会残留旧恢复上下文

复验目标：

1. 恢复入口进入 `任务中心` 时仍应保留恢复上下文
2. 用户改走普通侧边栏导航后，旧恢复上下文应被清理，不能继续污染普通任务浏览

真浏览器路径：

1. `三个和尚 -> 创作画布 Beta -> 镜头 3 -> 去任务中心继续回收视频`
2. 确认出现 `恢复上下文`
3. 再点击侧边栏 `项目控制台`
4. 再点击侧边栏 `任务中心`

复验结果：

1. 第一次通过恢复入口进入时：
   - 能看到 `恢复上下文`
   - 能看到 `第 1 集 / 镜头 3 / 视频回收`
2. 通过普通侧边栏重新进入 `任务中心` 后：
   - `恢复上下文` 不再显示
   - 页面恢复为普通任务中心视角

结论：

- 通过
- 当前系统已经区分：
  - `带恢复上下文的任务进入`
  - `普通任务浏览进入`
- 这让任务中心的恢复语义不再粘连，有利于后续继续做生产级调度体验

## 2026-07-29 补充复验：任务中心恢复成功后的结果闭环提示

本轮补充目标：
1. 验证恢复任务成功后，即使原任务从列表中移除，任务中心仍会保留“下一步去哪”的结果引导。
2. 避免成功提示只绑定在“当前选中的恢复任务卡片”上，导致列表刷新后提示消失。

涉及文件：
- `web/src/components/ProductWorkspaceTasksSection.tsx`
- `web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx`
- `web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.test.tsx`

实现说明：
1. 在 `ProductWorkspaceTasksSection.tsx` 新增 `taskOperationSummary` 状态，作为任务中心级别的操作总结。
2. 在三类恢复成功分支写入结果总结：
   - `recovery-regenerate-latest`
   - `result.outcome === "restarted"`
   - `result.statusPayload.status === "done"`
3. 在右侧详情面板顶部渲染独立的成功引导条，支持：
   - `前往镜头工作台`
   - `在创作画布查看`

构建与测试：

```bash
cd web
npm run test -- productWorkspaceTaskCenterSelectedTaskPanel
npm run build
```

结果：
- 单测通过
- 构建通过

真浏览器复验：
- 前端：`http://127.0.0.1:5175`
- 项目：`三个和尚`
- 路径：`正式产品工作台 -> 任务中心`
- 方式：
  1. 注入 `shot-execution-summary` 与 `pending-storyboard-tasks` 本地运行态
  2. 拦截 `GET /api/prototyping/tasks/runtime-verify-pending-001`
  3. 让任务状态按 `running -> running -> done` 返回

验收结果：
1. 恢复任务成功回写后，原恢复卡片可从列表中消失。
2. 右侧主面板即使跳转到别的任务，顶部仍保留：
   - `建议回看结果并确认下游`
   - `前往镜头工作台`
   - `在创作画布查看`
3. 证明“恢复成功后的下一步引导”已经从“选中卡片状态”提升为“任务中心操作结果状态”。

结论：
- 通过
- 任务中心的恢复链路现在已经具备更稳定的结果收口能力，更接近 LibTV 式“恢复动作 -> 结果回写 -> 明确下一步”的连续调度体验。
