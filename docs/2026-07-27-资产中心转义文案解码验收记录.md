# 2026-07-27 资产中心转义文案解码验收记录

## 验收目标

- 资产中心不应向用户直接展示 `\uXXXX` 形式的转义字符串。
- 人物、场景、道具资产卡片及详情中的造型/描述字段应显示正常中文。
- 修复范围应尽量收敛在资产展示归一化层，不影响任务链路和提示词链路。

## 本次调整

- 文件：`web/src/components/productWorkspaceAssets.ts`
- 调整点：
  - 在 `normalizeDisplayText(...)` 中补充转义 Unicode 文本解码。
  - 当字段值包含 `\u7070\u8272` 这类转义片段时，展示层会自动还原为正常中文。

## 自动化验证

- `npx vitest run src/components/productWorkspaceAssets.test.ts src/components/ProductWorkspaceAssetsSection.test.tsx`
  - 结果：通过
- `npm run build`
  - 结果：通过

## 新增回归测试

- 文件：`web/src/components/productWorkspaceAssets.test.ts`
- 覆盖点：
  - 当 `refined_outfit` 等资产详情字段存储为 `\uXXXX` 转义文本时，`detailTertiary` 会输出正常中文。

## 真浏览器验收

### 环境

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765](http://127.0.0.1:18765)

### 验收路径

1. 打开正式产品工作台
2. 进入项目 `三个和尚`
3. 进入 `资产中心`
4. 检查人物资产列表与详情中的造型字段

### 页面实际结果

- 页面中不再出现 `\u7070\u8272...` 形式的裸转义文案。
- 资产中心已能直接显示正常中文，如：
  - `灰色棉麻僧袍`
- 程序化检查结果：
  - `hasEscapedUnicode = false`
  - `hasDecodedText = true`

## 验收结论

- 通过。
- 资产中心的人物造型、状态描述和类似详情字段已经恢复为可读中文，真实用户浏览时不会再误以为数据损坏。

## 后续建议

- 继续排查正式工作台其余模块是否仍有历史转义文本残留，优先检查镜头工作台、QA 修复和导出中心中的资产描述回显。
