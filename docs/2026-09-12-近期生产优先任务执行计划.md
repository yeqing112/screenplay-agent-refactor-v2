# 近期生产优先任务执行计划

## 2026-09-17 Targeted Semantic Evidence Resolution（Provider-free 阶段）

- [x] 按 MissingFactManifest 检索候选 immutable source anchors，并保留 exact text、hash 与字符/字节偏移。
- [x] 增加结构化 semantic proposal、保守自然语言/代词承接解析、exact evidence 与 semantic support 校验。
- [x] 解析结果通过既有 FactSnapshot merge 与 Fact Coverage recheck；不改变阈值，不绕过 ScriptIR gate。
- [ ] 当前 6 项 required facts 需以真实可定位源证据复核；未满足时保持 `FACT_COVERAGE_INSUFFICIENT`。

## 2026-09-17 Targeted Missing Fact Extraction（已完成 Provider-free 阶段）

- [x] 建立 MissingFactManifest 与五类缺失原因。
- [x] 建立定向、范围受限的候选提取；无可靠 evidence 时 unresolved，不猜测。
- [x] 建立 evidence/schema/locator/authority/conflict 校验与 FactSnapshot merge。
- [x] 完成 coverage recheck 与 ScriptIR fail-closed gate 接线。
- [x] 当前证据：6 required、0 candidate、6 unresolved、`FACT_COVERAGE_INSUFFICIENT`、`BLOCKED_PENDING_TARGETED_MISSING_FACTS`、Provider calls=0。
- [ ] 下一步仅在补充真实可定位 source evidence 或获得独立语义验证授权后继续；不得降低阈值或绕过 gate。

日期：2026-09-12

## 当前结论

项目当前仍处于内测阶段。自动化回归、Golden fixtures 和正式工作台真浏览器巡检已通过，但生产级放行还缺少真实样本覆盖和真实媒体链路证据。

当前数据库与样本注册表核验结果：

- active 样本：`990400 / 第1集 / 镜头1–3`
- 可拍性与节拍回放：`3/3 pass`
- 30 镜头数量门禁：`3/30`，明确未通过
- 本轮未创建镜头、未复制历史镜头、未调用外部模型

## 当前执行看板（2026-09-12）

| 优先级 | 任务 | 当前状态 | 下一动作 | 放行证据 |
| --- | --- | --- | --- | --- |
| P0-1 | 真实样本与可拍性 | 阻断：1 个 active 项目、3 个真实镜头 | 准备并登记至少 3 个真实项目，合计至少 30 个真实镜头；补齐 `needs_information` 与 `conflict` 状态 | `validate:sample-registry`、`audit:shot-planning:gate` |
| P0-2 | 资产治理与 Prompt Compiler 灰度 | 代码与隔离回归已通过，真实资产灰度待执行 | 选定真实锁定资产，先预览证据包，再显式确认真实 LLM；只保存候选草案 | 真实调用审计、草案指纹、人工确认记录 |
| P0-3 | 图片与视频媒体链路 | 阻断：参考图尚未发布为 provider 可访问 URL | 用户确认后发布参考图，执行 GPT Image 2，再执行 MiniMax H3 多参考灰度 | 图片/视频任务、回收文件、版本、QA 与连续性记录 |
| P0-4 | 三个项目写入型主链路 | 待样本准备 | 每个真实项目完成剧本→分镜→资产→Prompt→图片→视频→QA→导出 | 真浏览器写入型 E2E 报告 |
| P0-5 | 生产安全与恢复 | 隔离 staging 通过；本地仍为 development | 部署时填入真实认证、HTTPS CORS、限流、稳定 HTTPS 对象存储域名并执行恢复演练 | `config:verify:production`、备份/恢复报告 |

当前可以继续自动执行的是代码回归、只读门禁和证据校验；真实样本内容、生产密钥、稳定域名及付费媒体调用必须由操作者提供或显式确认，系统不会用历史数据凑数或自动放行。

### 本轮推进记录

- 已完成批量样本登记器的通用化：`--book-id` 可重复传入，整批项目先校验，确认后才一次性写入。
- `pytest -q tests/test_production_sample_registry.py`：`12 passed`。
- `990401` + `990402` dry-run 已验证 fail-closed：两项目均无 `books`/剧本/镜头证据，`would_write=false`、`written=false`，注册表未改变。
- 当前没有新的真实项目证据，因此不执行确认写入；下一步仍是取得真实项目后再用同一批量流程登记。
- 变更后的 `npm run check:production` 已复验通过：后端 `648 passed`、Golden `5/5`、发布门禁不变量、运行时配置检查和前端生产构建均通过；本轮未触发外部付费调用。
- 最新 `npm run gate:production` 报告：`artifacts/production-release-gate-2026-09-12T12-03-24-007Z.{json,md}`。确定性回归与样本注册表通过；生产配置、真实镜头/状态覆盖、Prompt 覆盖和至少 3 个真实项目的真浏览器门禁继续 fail-closed。
- H3 只读预检最新报告：`artifacts/minimax-h3-gray-2026-09-12T12-06-36-815934Z.{json,md}`。`990400 / E1 / S1` 的分镜 6 秒正确映射，2 张锁定多参考已识别；因参考图尚未发布为 provider 可访问 URL，`0/2` 可提交，系统未触发外部调用。
- 定时数据库备份与恢复演练已复验成功：`artifacts/database-backups/screenplay-20260912T120754Z.sqlite`，源库/备份/恢复库完整性均为 `ok`，48 张表一致，`source_mutated=false`，临时恢复库已清理。
- `npm run test:release-gate` 已在本轮改动后复验通过，生产门禁仍保持 fail-closed 不变量。
- 本地运行态复核通过：API `127.0.0.1:18765/health` 与 Web `127.0.0.1:5175/` 均返回 HTTP 200，前后端服务可用于后续真实样本验收。
- 已生成 `990400` 资产只读清单 `artifacts/990400-visual-assets-preflight.json`：当前锁定场景参考图 asset `13`、人物“林晚”参考图 asset `8` 均只有本地媒体地址；发布这两张锁定参考图后，H3 预检才会从 `0/2` 进入可提交状态。
- 本机读取 asset `13` 与 `8` 的媒体端点均返回 HTTP 200 和 `image/png`；当前媒体阻塞不是文件丢失，而是供应商无法访问 `127.0.0.1`/相对路径。
- 对象存储迁移只读计划已保存为 `artifacts/990400-storage-migration-plan-preflight.json`：共扫描 12 个资产，全部需要发布；七牛凭据已配置，但私有 bucket + 临时 HTTP 域名使 `migration_apply_supported=false`。绑定稳定 HTTPS 域名并确认公开读取策略后，才能执行受保护迁移。
- 七牛临时域名只读 HEAD 返回 HTTP `401`（域名可达但 bucket 私有），证明需采用签名 URL 或公开读取策略；单纯替换为临时域名不足以让 H3 读取参考图。
- H3 灰度脚本已补通用 `--allow-unstable-public-assets` 请求级开关，并将该选择传入参考图/首帧发布和提交请求；该开关只适用于显式灰度，不改变生产域名门禁。相关回归 `16 passed`，本轮未执行外部上传或供应商调用。
- 新开关只读预检报告：`artifacts/minimax-h3-gray-2026-09-12T12-18-12-742390Z.{json,md}`。未启用发布时仍保持 `0/2` 参考图可提交，说明下一步必须由操作者显式确认发布参考图。
- 临时域名灰度开关改动后的完整 `npm run check:production` 已通过：后端 `649 passed`、Golden `5/5`、发布门禁不变量、运行时配置检查和前端构建均通过；真实样本与生产环境门禁状态未被放宽。
- H3 灰度发布路径已统一强制 `force_storage=true`；未显式传入临时域名灰度开关时，即使启用发布参数也会被安全门拒绝，避免绕过生产存储约束。
- H3 脚本已增加独立对象存储确认令牌 `MINIMAX_H3_STORAGE_CONFIRM=CONFIRM_MINIMAX_H3_REFERENCE_PUBLISH`；未确认时不会上传参考图，只返回阻塞原因。专项回归 `4 passed`。
- 对象存储二次确认与强制安全门改动后的完整 `npm run check:production` 已通过：后端 `650 passed`、Golden `5/5`、发布门禁不变量、运行时配置检查和前端构建均通过；真实样本/生产配置门禁仍未放宽。
- 未设置存储确认令牌时请求临时域名灰度发布的预检已验证 fail-closed：`storage_performed=false`、`actual_provider_submission=false`，报告 `artifacts/minimax-h3-gray-2026-09-12T12-33-21-736253Z.json`。
- 存储发布确认门新增回归后的完整 `npm run check:production` 已通过：后端 `651 passed`、Golden `5/5`、发布门禁不变量、运行时配置检查和前端构建均通过；真实样本与生产环境阻塞仍保持不变。
- 正式工作台 H3 UI 专项回归通过：机器提示词导出面板与分镜工作台共 `39 tests passed`，临时七牛开关、多参考模式及提交摘要交互无回归。
- H3 真实提交确认框已补充对象存储上传提示，并区分临时七牛灰度与稳定存储；前端专项测试 `39 passed`、生产构建通过。
- `npm run e2e:business` 隔离业务回归通过：正式工作台主链路、QA 修复/回滚和验收记录均通过，临时 fixture `999902` 已清理，未产生外部调用。
- `npm run e2e:machine-prompt-export` 隔离真浏览器回归通过：H3 多参考字段、WebUI 导出及编辑后导出均通过，临时 fixture `999903` 已清理。
- 当前真实样本只读真浏览器巡检通过：`990400` 的 10 个正式工作台模块可达、无业务阻塞、未写库；报告 `artifacts/e2e-real-sample-regression-2026-09-12T12-49-26-481Z.json`。仍需至少 3 个真实项目才能通过 release 版门禁。
- 正式工作台媒体预检已通过临时灰度路径：`990400 / E1 / S1` 返回 `ready_for_real_submit=true`，参考图可通过七牛发布；预检保持 `mutated=false`、`provider_call=false`。证据 `artifacts/990400-h3-media-preflight-unstable.json`。

样本注册校验还会报告孤儿证据（当前为 `991119`）：它没有对应的 `books` 主记录，因此被明确排除，不应通过注册命令激活；如需清理，必须另行执行有审计记录的数据库清理流程。

生产限流门禁新增 `API_RATE_LIMIT_DISTRIBUTED_ASSERTED`。该变量只代表共享/反向代理限流已经实际配置，不会在应用内伪造分布式能力；未声明时 production/staging 配置校验会 fail-closed。

模板占位值防假绿规则上线后，完整确定性回归最近为 `648 passed`、Golden `5/5`、前端生产构建通过；该结果不替代真实样本、媒体链路和部署环境门禁。
- 2026-09-12 已补通用 API 限流边界：通过 `API_RATE_LIMIT_ENABLED` 显式开启后，对 `/api/*` 按调用方 token 哈希或客户端 IP 施加固定窗口限流，返回标准 `429`、`Retry-After` 与 `X-RateLimit-*` 信息；健康检查和 CORS 预检不计数。该实现是单进程应用层兜底，多进程生产环境仍必须在反向代理/共享限流器配置分布式策略。
- 2026-09-12 最新 `npm run check:production` 已通过：后端 `643 passed`、Golden `5/5`、运行时配置检查通过、前端生产构建通过；质量门禁仍因真实 active 镜头只有 `3/30` 而 fail-closed。
- 新增统一 `npm run gate:production`：依次执行生产配置、样本注册表、可拍性门禁、Prompt 门禁和完整回归；所有步骤都会运行并在 `artifacts/production-release-gate-*.{json,md}` 留存结果，任一步骤失败即 fail-closed。
- 新增 `npm run test:release-gate`，锁定发布门禁在开发环境下不能把生产配置检查的“跳过”误判为通过。

## 执行顺序

### P0-1：真实样本与可拍性门禁

状态：进行中，等待真实项目样本补充。

要求：

1. 由真实项目产生至少 30 个 active 镜头；不得通过复制、伪造或历史脏数据凑数。
2. 每个镜头具有导演分镜、结构化动作、时长及必要的起止状态证据。
3. 执行 `npm run audit:shot-planning:gate`，同时覆盖 `ready`、`needs_information`、`conflict`。
4. 将缺失信息和时长冲突回归到正式镜头工作台，确认按钮只引导补证据，不绕过门禁。

### P0-2：资产与 Prompt Compiler 受控灰度

状态：Prompt Compiler 已完成 clone-only 样本；资产语义治理仍需至少一组真实 LLM 灰度。

要求：

- 先生成只读证据包，再由用户显式确认真实调用。
- LLM 只保存候选草案，不直接改资产、镜头或 Prompt Version。
- 人工确认时校验来源快照、锁定资产和证据指纹；变化则使草案过期。

### P0-3：真实图片与视频媒体链路

状态：等待真实外部调用确认与可访问参考图。

顺序：

1. 选定已锁定资产，完成 GPT Image 2 分镜图灰度。
2. 验证图片回收、本地持久化、版本记录和任务回填。
3. 使用锁定多参考图执行 MiniMax H3 灰度。
4. 验证时长映射、供应商轮询、失败恢复、视频回填、连续性证据和 QA 结果。

临时七牛域名灰度必须同时显式设置 `MINIMAX_H3_STORAGE_CONFIRM=CONFIRM_MINIMAX_H3_REFERENCE_PUBLISH`、`MINIMAX_H3_GRAY_REAL=1`、`MINIMAX_H3_GRAY_CONFIRM=CONFIRM_MINIMAX_H3_SUBMIT`，并传入 `--publish-reference-images --allow-unstable-public-assets --allow-real`；缺少任一项时不会上传或提交。

临时七牛域名只允许灰度；生产放行前必须绑定稳定 HTTPS 自定义域名。

### P0-4：三个真实项目写入型主链路

状态：待样本准备。

主链路：

`内容准备 → 改编方向 → 剧本 → 分镜 → 资产 → Prompt Version → 分镜图 → 视频 → QA → 导出`

验收要求：不改数据库、不手工改 JSON，所有写入均有审计和回滚线索。

### P0-5：生产安全与恢复

状态：需发布环境复核。

- HTTPS 公网资产域名
- 认证与授权
- CORS 白名单
- 上传类型、大小和路径校验
- 限流、备份、健康检查
- 任务失败重试与恢复
- 生产数据库并发方案

## P1：P0 通过后推进

1. 多变体资产继承与 stale Prompt 修复。
2. 对象存储受保护迁移执行器（dry-run、确认令牌、记录、重试、回滚）。
3. Luma、Veo、Runway 等视频模型字段级真实灰度。
4. 评估并逐步开启默认 ShotPlan 强门禁。
5. 旧执行层接口与历史模型兼容收尾。
6. 键盘、窄屏、长名称和真实创作者可用性复核。

## Agent 后续阶段

Agent 不替代正式工作台门禁，待 P0 主链路稳定后继续：

1. 普通用户自由对话、图片/文档附件和主动项目汇报。
2. 独立模型、思考/图像识别策略、预算和用量观测。
3. 首批 Skill 的真实样本验收与受控扩展机制。
4. Agent 操作审计、回放和 provenance 交付清单。

## 本轮证据

- 已将 `audit:shot-planning:gate` 收口为通用状态覆盖门禁：除至少 30 个真实 active 镜头外，还要求样本同时覆盖意图 `ready / needs_information` 与节拍 `ready / conflict`；缺少任一状态会写入报告并 fail-closed，不依赖书号、角色或镜头特例。
- 已补充默认关闭的通用 API Token Guard：部署显式开启后统一保护 `/api/*`，只放行健康检查、配置的公开路径和 CORS 预检；短 token 或未配置时 fail-closed，不改变本地开发行为。细粒度角色授权仍列为生产安全后续项。
- 已新增显式 SQLite 备份/校验工具 `npm run backup:database -- --output <path>`：使用在线 backup API、源库与目标库完整性校验、临时文件原子替换，默认拒绝覆盖且不会删除或修改源库；专项回归覆盖源库不变、覆盖保护及非 SQLite 拒绝。
- 已增加跨平台 Node 参数转发入口，修复 Windows npm 对 `--output` 的转发差异；命令已实跑创建并校验 `artifacts/production-preflight-20260912-npm.sqlite`，源库未变更。
- 新增 `verify:database-restore` 非破坏性恢复演练：把备份恢复到临时 SQLite，校验完整性与表清单后自动清理临时库；专项回归覆盖源备份不变。
- 已新增可选 API 限流配置：`API_RATE_LIMIT_ENABLED`、`API_RATE_LIMIT_REQUESTS`、`API_RATE_LIMIT_WINDOW_SECONDS`、`API_RATE_LIMIT_MAX_IDENTITIES`；默认关闭，不改变本地开发行为。专项安全回归覆盖限流返回头、429 拒绝、健康检查和预检豁免。
- 已补可选角色令牌授权：`API_AUTH_ROLE_TOKENS` 使用 JSON 配置 `viewer / editor / admin`，统一按 HTTP 方法施加只读/写入最低权限；未知角色或弱令牌 fail-closed，旧单 Token 继续按 admin 兼容。专项回归覆盖 viewer 写入拒绝、editor 放行和非法角色配置拒绝。
- 新增 `npm run config:verify:production`：仅在 `DEPLOYMENT_ENV=production|staging` 时强制检查 Token/角色、HTTPS CORS、API 限流及稳定对象存储域名；开发环境明确标记 skipped，不改变本地流程。
- 已修正运行配置读取 `DEPLOYMENT_ENV` 的遗漏；安全配置脚本已验证开发环境跳过、生产安全配置通过、缺失/临时域名配置拒绝三条路径。
- 当前本地配置以 `DEPLOYMENT_ENV=production` 实跑仍 fail-closed，缺口为认证/角色、HTTPS CORS、限流、对象存储 provider 与稳定 HTTPS 公网地址；该失败是上线门禁预期行为，不修改本地开发默认值。
- 生产配置放行检查同时要求 `ENABLE_LEGACY_NODE_API=false`，防止旧执行层在正式部署中继续暴露；开发环境仍可保持兼容开关。
- 最新完整生产回归已实跑通过：后端 `626 passed`（863 warnings，均为既有弃用/测试告警）、Golden `5/5`、运行时配置检查通过、前端生产构建通过；质量门禁仍因真实镜头样本不足而独立 fail-closed。
- 追加角色/旧执行层门禁后的最新完整回归：后端 `627 passed`、Golden `5/5`、配置检查与前端生产构建均通过；质量门禁仍独立保持 fail-closed。
- 加入可拍性门禁报告动作字段后的最新完整 `check:production`：后端 `629 passed`、Golden `5/5`、配置检查与前端生产构建均通过；质量门禁仍独立保持 fail-closed。
- 加入生产样本注册表校验后的最新完整 `check:production`：后端 `632 passed`、Golden `5/5`、配置检查与前端生产构建均通过；质量门禁仍独立保持 fail-closed。
- 新增《生产部署放行运行手册》，固化生产变量、放行顺序、备份恢复、反向代理和上线后抽查要求；不改变本地开发默认配置。
- 可拍性回放报告新增通用 `release_blockers` / `next_actions` 字段及 Markdown 汇总，当前实跑会明确列出镜头数量不足、缺少 `needs_information` 与 `conflict` 的阻塞原因和正式工作台补证据动作；不改变门禁判定。
- 新增只读 `npm run validate:sample-registry`，校验 active/retired 不重叠且 active 项目确实存在剧本与镜头证据；当前 `990400` 校验通过（1 个剧本、3 个镜头）。
- 修正场景资产前置审计默认范围：不再扫描硬编码历史书号，默认只读取 active 样本注册表；显式 `SCENE_ASSET_AUDIT_BOOK_IDS` 仍可用于受控历史取证。
- `npm run audit:shot-planning:gate`：报告已生成，因 `3/30` 返回非零，门禁行为正确。
- `npm run check:production`：后端 `609 passed`、Golden `5/5`、配置检查和前端生产构建通过。
- `npm run e2e:business`、`npm run e2e:real-samples`、`npm run e2e:agent-browser`：最近一次隔离/真浏览器回归通过。
- 修复 `validate-minimax-h3-gray.py auto` 的候选范围：未传候选项目时现在只读取 `production-sample-registry.json` 的 `active_book_ids`，不再扫描数据库中的历史/退休项目；当前只读预检正确选中 `990400 / 第1集 / 镜头1`，仍因参考图不可被供应商访问而阻断，且未调用供应商。
- 真浏览器真实样本回归同步采用 active 注册表范围；`npm run e2e:real-samples` 当前明确只选择 `990400`，10 个正式工作台模块通过且无业务写入。
- 只读接口复核 `GET /api/books/990400/production-readiness` 返回 `pass`（3 镜、2 资产、0 blocker、0 warning）；这只证明该项目局部就绪，不替代全局 30 镜及状态覆盖门禁。
- 提示词零错误门禁的默认最小抽检量已与 P0 验收统一为 30 个真实镜头（仍可通过 `AUDIT_STORYBOARD_MIN_AUDITED_SHOTS` 提高）；当前 3 个镜头因此明确返回未通过，不会把 `0 error / 0 warning` 误判为生产绿灯。
- 最新完整 `check:production`（2026-09-12）结果为后端 `635 passed`、Golden `5/5`、运行时配置检查通过、前端生产构建通过；该结果不替代真实样本数量、异常状态覆盖和媒体供应商灰度门禁。
- 统一 `gate:production` 已实跑：完整回归步骤通过；发布配置、可拍性与 Prompt 覆盖门禁按当前本地事实 fail-closed（开发环境、3/30 镜头、缺少 `needs_information`/`conflict`），报告写入 `artifacts/production-release-gate-2026-09-12T09-33-14-720Z.{json,md}`。
- 统一门禁最新实跑报告为 `artifacts/production-release-gate-2026-09-12T10-04-52-792Z.{json,md}`：阻塞原因已结构化展示；完整回归步骤通过（638 passed、Golden 5/5），质量门禁仍为 3/30。
- `npm run test:release-gate` 已通过，确保生产配置检查在开发环境“跳过”时不会被发布门禁误判为绿灯。
- `check:production` 已纳入 `test:release-gate` 轻量不变量回归，后续完整生产回归会持续验证发布门禁的 fail-closed 规则。
- 定时备份入口加入后的最新完整回归为后端 `641 passed`、Golden `5/5`、运行时配置检查和前端生产构建通过；样本治理与备份逻辑未改变生产主链路。
- 最新统一门禁报告为 `artifacts/production-release-gate-2026-09-12T10-26-02-402Z.{json,md}`，结构化阻塞原因与前述结论一致，未产生外部调用。
- 2026-09-12 10:36 再次执行 `npm run gate:production`：完整回归仍为 `641 passed`、Golden `5/5`、前端构建通过；发布配置仍因当前环境为 `development` 被阻断，可拍性与提示词质量门禁仍因真实样本仅 `3/30` 及缺少 `needs_information/conflict` 被 fail-closed。报告：`artifacts/production-release-gate-2026-09-12T10-36-42-760Z.{json,md}`。本轮未产生外部调用。
- 定时备份入口已补跨平台 Node 转发；Windows npm 会吞掉未知长参数，因此新增 `npm run backup:database:scheduled:restore-drill` 作为稳定的恢复演练入口。通过该入口验证时仍要求 `source_mutated=false`、源/备份/恢复库完整性为 `ok`，并保留独立时间戳备份。
- 2026-09-12 10:41 通过新的 `npm run backup:database:scheduled:restore-drill` 实跑成功：48 张表、源库/备份/恢复库完整性均为 `ok`，`source_untouched=true`，临时恢复库已清理。
- 生产安全配置预检（临时进程设置 `DEPLOYMENT_ENV=production`，未修改本地环境）已明确 7 项待配置项：API 认证、强 Token/角色令牌、HTTPS CORS、启用限流、关闭旧节点 API、对象存储 provider、稳定 HTTPS 公网资产 URL。
- 修正真实 Prompt Compiler 灰度脚本默认范围：自动模式现读取 `production-sample-registry.json` 的 active 项目，不再隐式扫描旧书号；新增选择回归 `tests/test_real_llm_gray_selection.py`（2 passed）。mock 安全路径已用 `990400 / 1:1` 克隆验证通过并清理临时项目，报告：`artifacts/storyboard-real-llm-gray-20260912T104649Z.{json,md}`；未调用外部 LLM。
- 2026-09-12 10:55 变更后完整 `npm run check:production` 通过：后端 `643 passed`、Golden `5/5`、运行时配置检查与前端生产构建通过；该结果不改变真实样本与生产安全配置门禁的阻塞状态。
- 2026-09-12 10:57 最新 `npm run gate:production` 已完成：完整回归 `643 passed`、Golden `5/5`、前端构建通过；统一报告为 `artifacts/production-release-gate-2026-09-12T10-57-48-356Z.{json,md}`。发布配置、真实样本数量与状态覆盖仍按预期 fail-closed，未产生外部调用。
- 2026-09-12 11:00 正式工作台真浏览器只读回归通过：仅发现已注册的 `990400`，10 个模块可达、无业务阻塞、未写库；报告：`artifacts/e2e-real-sample-regression-2026-09-12T11-00-25-207Z.json`。该结果不替代 30 镜头门禁。
- 2026-09-12 11:01 H3 只读预检自动选中 `990400 / 第1集 / 镜头1`，分镜时长正确映射为 6s，锁定多参考 2 张；未调用供应商。当前唯一媒体阻塞为两张参考图尚未发布为 provider 可访问 URL，预检报告：`artifacts/minimax-h3-gray-2026-09-12T11-01-46-826413Z.{json,md}`。下一步需在页面显式确认后执行参考图公网中转/真实 H3 灰度。
- 新增 `npm run e2e:real-samples:release`：在不改变本地开发回归自适应行为的前提下，强制至少 3 个真实项目样本，作为生产放行前的真浏览器门禁入口。
- 该入口已验证 fail-closed：当前只有 1 个真实项目时默认返回 `Only found 1 real samples, expected at least 3`；仅在显式临时 smoke 覆盖为 1 时通过，避免生产假绿。
- 统一 `gate:production` 已纳入 `real_browser_release_e2e` 步骤；生产门禁现在同时要求确定性回归和至少 3 个真实项目的真浏览器证据。
- 生产配置“通过路径”已在隔离 staging 进程中验证：认证、HTTPS CORS、限流、关闭旧节点 API、对象存储 provider/HTTPS 域名等占位配置全部 `ok`；未修改本地 `.env`，未触发外部调用。正式部署仍需替换为真实密钥和域名。
- 只读核对当前数据库：共 4 个历史书籍记录，但仅 active `990400` 同时具有剧本证据（1）和镜头（3）；`990301/990306/990307` 没有剧本来源，不得作为真实生产样本补数。
- 样本注册表校验现在会只读列出“有剧本和镜头、但尚未注册”的候选项目（`available_candidates`），不会自动激活或改变项目状态。
- 新增受控真实样本登记命令 `npm run register:production-sample`：默认 dry-run，必须显式 `REGISTER_PRODUCTION_SAMPLE` 才能原子更新注册表；不会创建/复制镜头，退休项目需额外 `--unretire`。
- 新增 `npm run backup:database:scheduled` 时间戳备份入口：每次生成新文件并自动完整性校验，可选非破坏性恢复演练；不覆盖、不删除、不上传旧备份。
- 生产安全校验现在拒绝模板密钥、弱占位值和 `localhost`/`example.com`/`example.org` 示例域名；并要求 `API_RATE_LIMIT_DISTRIBUTED_ASSERTED=true` 作为共享限流已部署的显式运维证据。相关正反例与 staging 通过路径已验证。
- 2026-09-12 20:58 再次执行 `npm run gate:production`：确定性回归 `651 passed`、Golden `5/5`、前端生产构建通过；统一门禁仍按事实 fail-closed。当前阻塞保持为开发环境、真实镜头 `3/30`、缺少 `needs_information`/`conflict`、Prompt 审计仅覆盖 3 镜头，以及真浏览器生产样本仅 1 个。
- 2026-09-12 20:59 执行 `STORYBOARD_REAL_LLM_GRAY_MOCK=1 npm run validate:storyboard-real-llm-gray`：`990400 / E1 / S1` clone-only 预检通过（1/1），临时项目已清理；未调用真实 LLM、未写入真实项目。
- 2026-09-12 20:59 执行 H3 只读媒体预检：自动选中 `990400 / E1 / S1`，分镜时长映射为 `6s`，识别到 `2` 张锁定多参考；因参考图仍不是 provider 可访问的公网 URL，`ready_for_real_submit=false`，未上传对象存储、未调用 H3。最新证据：`artifacts/990400-h3-media-preflight-latest.json`。
- 已新增可执行清单 `artifacts/production-next-actions-20260912.md`，把真实样本补充、状态覆盖、灰度确认、H3 媒体验证和最终放行命令按顺序固化；不改变门禁阈值或本地默认配置。
- 2026-09-12 21:04 执行 `npm run e2e:real-samples`：当前 active 样本 `990400` 的 10 个正式工作台模块全部可达、`blocked=none`，未产生业务写入；该只读回归不替代生产版至少 3 个真实项目门禁。证据：`artifacts/e2e-real-sample-regression-2026-09-12T13-04-47-745Z.json`。
- 2026-09-12 21:25 创建原创测试小说《潮汐回声》，通过正式上传/导入路径建立新项目 `990401`；导入后校正了仅由文件顶部书名产生的无意义前置章节，当前为 10 个内容章节、2,648 字，状态为 `imported`。该项目尚未注册为 active 样本，也未调用任何外部模型。

## 不允许的捷径

- 不复制镜头或伪造真实样本。
- 不用审计白名单掩盖 warning/error。
- 不自动调用付费模型、图片/视频供应商或对象存储。
- 不擅自切换模型，不用特例规则替代通用证据。
