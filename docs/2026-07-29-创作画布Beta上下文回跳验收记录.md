# 2026-07-29 创作画布 Beta 上下文回跳验收记录

## 本轮目标

继续按 LibTV 化融合计划推进 `创作画布 Beta`，把它从“可看全局”进一步推进到“可从局部工作台带着上下文回到全局图谱”。

本轮重点：

1. 从镜头工作台进入创作画布
2. 从资产中心进入创作画布
3. 进入后尽量保持上下文语义，而不是只切换到画布页签

## 本轮实现

### 1. 新增画布导航目标状态

在正式工作台内部新增 `canvasNavigationTarget`，用于承接：

- `episode`
- `shotId`
- `assetId`
- `assetLabel`
- `taskId`

相关文件：

- [ProductWorkspace.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspace.tsx)
- [productWorkspaceAssetViewController.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceAssetViewController.ts)
- [productWorkspaceSectionContracts.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceSectionContracts.ts)
- [productWorkspaceSectionBundlesController.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceSectionBundlesController.ts)

### 2. 创作画布支持目标节点解析

新增基于真实图谱节点的目标解析逻辑：

- 优先按镜头定位
- 其次按资产定位
- 最后按分集回落到剧本节点

相关文件：

- [productWorkspaceCanvasBeta.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceCanvasBeta.ts)
- [ProductWorkspaceCanvasBetaSection.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspaceCanvasBetaSection.tsx)

### 3. 两个真实入口已接入

已新增显式入口：

- 镜头工作台：`在创作画布查看`
- 资产中心：`在创作画布查看`

相关文件：

- [ProductWorkspaceStoryboardSection.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspaceStoryboardSection.tsx)
- [ProductWorkspaceAssetsSection.tsx](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/ProductWorkspaceAssetsSection.tsx)

## 测试与构建

本轮已执行：

1. `npm run test -- productWorkspaceCanvasBeta`
2. `npm run build`

结果：

- 单测通过
- 构建通过

新增测试覆盖：

- 画布导航目标能解析回正确节点

测试文件：

- [productWorkspaceCanvasBeta.test.ts](D:/Work/Project/screenplay-agent-refactor-v2/web/src/components/productWorkspaceCanvasBeta.test.ts)

## 真浏览器验收

验收日期：2026-07-29

验收路径 1：

`三个和尚 -> 正式工作台 -> 镜头工作台 -> 镜头 8 -> 在创作画布查看`

结果：

- 成功进入 `创作画布 Beta`
- 页面保留第 1 集上下文
- 页面中可见 `镜头 8`
- 右侧 `节点详情` 与 `打开对应模块` 仍可见

验收路径 2：

`三个和尚 -> 正式工作台 -> 资产中心 -> 和尚丙 -> 在创作画布查看`

结果：

- 成功进入 `创作画布 Beta`
- 页面中可见 `和尚丙`
- 右侧 `节点详情` 与 `打开对应模块` 仍可见

## 验收结论

本轮说明：

1. 创作画布已经不再只是独立展板，而是开始接入正式工作流中的真实回跳链路。
2. 用户现在可以从局部工位回到全局图谱继续定位问题和查看依赖。
3. 这一步是 L1 向 L2 过渡的必要基础，因为后续可执行节点也必须依赖同样的上下文恢复机制。

## 下一步

建议继续按计划推进：

1. 接入 `任务中心 -> 创作画布` 的上下文恢复入口
2. 补节点分组/折叠，降低大图浏览成本
3. 进入 L2：提示词编译节点与单镜头生图节点的最小可执行闭环
