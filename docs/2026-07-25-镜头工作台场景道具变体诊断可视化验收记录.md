# 2026-07-25 镜头工作台场景道具变体诊断可视化验收记录

## 本次目标

把后端已经产出的下列诊断项，明确暴露到镜头工作台前端页面中：

1. `scene_variant_state`
2. `prop_variant_state`
3. 并补齐对应的“重新编译提示词”修复入口联动

---

## 实施结果

### 1. 前端可视化已补齐

已在以下文件中完成实现：

- `web/src/components/ProductWorkspaceStoryboardSection.tsx`

新增内容：

1. 新增专项诊断卡片生成逻辑 `buildCompilerDiagnosticFocusCards(...)`
2. 对以下失败检查项给出更可读的可视化标题与说明：
   - `character_variant_state`
   - `scene_variant_state`
   - `prop_variant_state`
3. 在“编译诊断”区域新增专项诊断卡片展示
4. “重新编译提示词”修复入口的触发条件，已扩展覆盖：
   - `authority_prompt_inheritance`
   - `visual_fact_target_coverage`
   - `character_variant_state`
   - `scene_variant_state`
   - `prop_variant_state`

### 2. 组件测试已补齐

已在以下文件中新增覆盖：

- `web/src/components/ProductWorkspaceStoryboardSection.test.tsx`

新增验收点：

1. 当 `scene_variant_state` 失败时，页面显示“场景变体继承缺失”
2. 当 `prop_variant_state` 失败时，页面显示“道具变体继承缺失”
3. 页面可展示对应 detail 中的关键视觉事实缺口
4. 页面同时保留“重新编译提示词”修复动作

---

## 本地验证

### 自动化测试

执行命令：

```bash
npm --prefix web run test -- ProductWorkspaceStoryboardSection.test.tsx
```

结果：

- 11 tests passed

### 前端构建

执行命令：

```bash
npm --prefix web run build
```

结果：

- 构建通过

---

## 真浏览器验收

验收地址：

- 前端：[http://127.0.0.1:5182](http://127.0.0.1:5182)

验收路径：

1. 进入项目 `三个和尚`
2. 打开 `正式工作台`
3. 进入 `镜头工作台`
4. 查看第 1 集镜头详情中的“编译诊断”区域

验收结果：

1. 页面可正常进入镜头工作台
2. 真实项目数据下，“编译诊断”区域可正常展示检查项、告警、修复入口
3. 当前真实样本镜头主要触发的是 `authority_prompt_inheritance`
4. 当前这批真实镜头数据中，**尚未实际出现** `scene_variant_state / prop_variant_state` 失败项，因此真浏览器中暂未看到这两张专项卡片
5. 这说明：
   - 前端显示能力已接通
   - 自动化测试已证明场景/道具专项卡片能正常渲染
   - 真实项目还需要后端产出实际命中的样本，才能在真实业务数据中直接看到这两类卡片

验收截图：

- `artifacts/acceptance-home-2026-07-25.png`
- `artifacts/acceptance-three-monks-workspace-2026-07-25.png`
- `artifacts/acceptance-three-monks-workspace-loaded-2026-07-25.png`
- `artifacts/acceptance-storyboard-workspace-2026-07-25.png`

---

## 结论

本项整改已完成到“可落地、可测试、可上线”的状态：

1. 后端新增的场景/道具变体诊断，前端已具备明确可视化能力
2. 相关修复动作已并入提示词重编译入口
3. 自动化测试与前端构建均通过
4. 真浏览器已确认镜头工作台链路正常

---

## 后续建议

下一步优先做两件事：

1. 在真实项目样本中制造或捕获一条 `scene_variant_state / prop_variant_state` 命中的镜头数据，补一轮完整真浏览器截图验收
2. 继续推进“真实提示词产物质量验收”，重点检查静态提示词是否真正吸收：
   - 场景状态
   - 高重要性道具状态
   - 人物当前分镜定妆状态
