# 2026-08-03 公共摘要测试基线清理与轻量复验记录

更新时间：2026-08-03（周一）

## 1. 目标

本轮目标是清理正式工作台公共摘要层的历史乱码测试基线，并用真实浏览器做轻量复验，确认页面摘要文案与测试断言保持一致。

## 2. 本轮处理范围

重建并清洗以下测试文件：

- `web/src/components/productWorkspaceProjectStatus.test.ts`
- `web/src/components/ProductWorkspaceSectionContent.test.ts`

处理内容：

1. 将项目状态映射测试从历史乱码断言切换为正常中文断言
2. 将创作画布跨模块承接摘要测试切换为正常中文断言
3. 保持已有行为覆盖，不改动模块职责与逻辑结构

## 3. 自动化回归

执行命令：

```bash
cd web
npm run test -- productWorkspaceProjectStatus ProductWorkspaceSectionContent
npm run build
```

结果：

- `productWorkspaceProjectStatus` 测试通过
- `ProductWorkspaceSectionContent` 测试通过
- `vite build` 通过

## 4. 真实浏览器轻量复验

验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765](http://127.0.0.1:18765)
- 项目：`三个和尚`（`bookId = 14`）

验收路径：

1. 首页
2. 进入 `三个和尚`
3. 打开正式产品工作台
4. 检查项目控制台摘要
5. 打开 `导出中心`
6. 检查交付摘要文案

### 复验结果

已确认：

- 项目控制台显示 `当前项目状态：剧本已完成`
- 页面未出现 `瀵`、`閫`、`鍐呭`、`Bible` 等历史脏文本
- `导出中心` 页面存在正常文案：
  - `导出中心`
  - `交付 readiness`
  - `交付包内容`

## 5. 验收结论

本轮公共摘要测试基线清理与轻量复验通过。

已经完成：

- 公共摘要测试从历史乱码断言中脱钩
- 项目状态摘要与跨模块承接摘要的测试基线已统一为正常中文
- 页面摘要文案与测试断言保持一致

## 6. 后续建议

1. 继续清理 `productWorkspaceDelivery.test.ts` 等仍残留大量历史乱码断言的测试文件
2. 保持“先修基线，再做真页面复验”的推进方式
3. 继续优先治理正式主链路高频模块
