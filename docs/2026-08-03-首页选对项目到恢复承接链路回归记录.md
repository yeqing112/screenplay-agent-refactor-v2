# 2026-08-03 首页选对项目到恢复承接链路回归记录

## 1. 验收目标

验证以下两个修复在真实用户路径中已经能够协同工作：

1. 首页同名项目识别修复
2. 任务中心到镜头工作台恢复承接提示修复

## 2. 验收路径

基于正式工作台同源本地恢复仓，向 `book 14 / 第 1 集 / 镜头 3` 注入一条 `video` 待回收任务，然后执行：

1. 打开首页
2. 确认同时存在：
   - `三个和尚 · #14`
   - `三个和尚 · #17`
3. 点击 `[data-book-id="14"]`
4. 进入正式产品工作台
5. 打开 `任务中心`
6. 点击 `视频结果回收`
7. 点击 `前往镜头工作台`

## 3. 真浏览器结果

### 首页命中

- `homeHas14 = 1`
- `homeHas17 = 1`

说明：

- 同名项目已经不会被首页合并
- 用户可以直接从首页选中正确的 `book 14`

### 正式工作台命中

- 已成功进入 `book 14 / 三个和尚` 的正式工作台上下文
- 可继续进入任务中心

### 恢复承接命中

- `videoRecoveryCount = 1`
- `hasTaskBanner = 1`
- `hasTaskTitle = 1`
- `hasDismiss = 1`

页面命中内容包括：

- `任务中心承接中`
- `已从任务中心定位到第 1 集 / 镜头 3`
- `关闭提示`
- 恢复任务 ID：`acceptance-video-task-002`
- 场景：`寺庙后院水房`

## 4. 验收截图

- `tmp-book14-selection-and-recovery-regression.png`

## 5. 结论

截至 2026-08-03：

- 首页已经可以稳定区分同名项目
- 用户能够明确点进 `book 14`
- 从正确项目进入任务中心后，恢复任务可以继续承接到镜头工作台
- 承接提示会稳定显示，不再丢失

这说明首页入口修复与恢复承接修复已经在真实用户链路里闭环。
