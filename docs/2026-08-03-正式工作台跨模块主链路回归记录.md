# 2026-08-03 正式工作台跨模块主链路回归记录

## 1. 验收目标

确认 `book 14 / 三个和尚` 在正式产品工作台中，一级模块已经具备跨模块连续可达性，并且每个模块都能命中真实项目上下文，而不是停留在空壳页面。

## 2. 验收路径

1. 打开首页
2. 选择 `[data-book-id="14"]`
3. 进入正式产品工作台
4. 依次切换以下一级模块：
   - 项目控制台
   - 内容准备
   - 改编方向
   - 剧本工作台
   - 镜头工作台
   - 创作画布 Beta
   - 资产中心
   - QA 修复
   - 任务中心
   - 导出中心
   - 模型管理

## 3. 真浏览器结果

### 首页与项目命中

- 已成功从首页选中 `book 14`
- 已进入 `三个和尚` 的正式工作台上下文
- 页面显示：`当前项目状态：剧本已完成`

### 各模块命中结果

#### 项目控制台

- 命中项目状态
- 命中 `24 镜`
- 命中资产统计信息

#### 内容准备

- 命中内容准备入口与内容相关信息

#### 改编方向

- 命中改编方向页与锁定状态信息

#### 剧本工作台

- 命中剧本工作台
- 命中 `第 1 集`

#### 镜头工作台

- 命中镜头 `3`
- 命中场景 `寺庙后院水房`

说明：

本轮是通过侧栏直接进入镜头工作台，因此**没有**出现 `任务中心承接中`，这属于预期行为；承接提示只应在从任务中心恢复链路进入时出现。

#### 创作画布 Beta

- 命中创作画布
- 命中镜头 / 场景上下文

#### 资产中心

- 命中资产中心
- 命中人物资产（如 `和尚甲 / 和尚乙 / 和尚丙`）

#### QA 修复

- 命中 QA 修复页
- 命中问题 / 阻塞相关信息

#### 任务中心

- 命中任务中心
- 命中 `视频结果回收`

#### 导出中心

- 命中导出 / 交付相关信息

#### 模型管理

- 命中模型管理页
- 命中模型 / provider / 供应商相关信息

## 4. 结论

截至 2026-08-03，正式产品工作台已经具备：

- 首页选中真实项目
- 进入正式工作台
- 在 11 个一级模块之间切换
- 命中真实项目上下文

这说明正式工作台当前已经不是“只有局部模块可用”的状态，而是具备了稳定的跨模块主链路骨架。

## 5. 验收截图

- `tmp-formal-workspace-cross-module-regression.png`
