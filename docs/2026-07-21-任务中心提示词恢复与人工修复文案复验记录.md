# 2026-07-21 任务中心提示词恢复与人工修复文案复验记录

## 验收环境

- 前端：`http://127.0.0.1:5177`
- 后端：`http://127.0.0.1:18767`
- 验收方式：Playwright 真浏览器操作
- 验收日期：2026-07-21

## 本轮目标

验证上一轮整改后的两件事是否真实成立：

1. 任务中心是否已经区分“推荐恢复已用尽，需要人工修复”
2. 镜头工作台是否移除了 `待恢复` 的乱码展示，并正确暴露当前无推荐恢复版本的状态

## 实际操作路径

1. 打开项目列表页
2. 进入 `三个和尚`
3. 进入 `正式工作台`
4. 切换到 `任务中心`
5. 点击 `前往镜头继续修复`
6. 落到 `镜头工作台` 的第 1 集镜头 1 详情

## 验收结果

### 1. 任务中心文案状态正确

真实页面中已看到：

- `待人工修复 1`
- `前往镜头继续修复`

这说明当前任务语义已经不再误导为“还有一键恢复”，而是明确告诉用户：推荐恢复链路已经跑完，剩余问题需要进入镜头工作台继续人工收敛。

### 2. 镜头工作台已不再出现 `待恢复` 乱码

本轮复验前，镜头列表里存在历史脏编码展示：`寰呮仮澶?`。

修复后再次进入真实页面确认：

- 页面文本中包含 `待恢复`
- 页面文本中不再包含 `寰呮仮澶`

说明 `web/src/components/productWorkspaceStoryboard.ts` 里的历史乱码状态文案已经被清理干净，并且前端展示已生效。

### 3. 当前“无推荐恢复版本”状态已被真实暴露

镜头工作台中确认到：

- `可恢复版本 0 个`
- 编译诊断仍存在待处理项
- 当前问题仍指向关键资产缺失与权威继承不足

这与当前产品语义一致：

- 推荐恢复已经执行过
- 本次恢复只做到了部分收敛
- 后续必须进入人工修复链路继续处理

## 本轮顺手修复

### 已修复

1. `buildShotReadiness` 的状态文案全部替换为正常中文：
   - `待恢复`
   - `阻塞`
   - `待补齐`
   - `可交付`
2. 镜头工作台上游放行提示文案全部改回正常中文
3. 对应测试断言同步修正，避免继续把乱码当作正确结果

### 验证通过

- `npm run test -- --run src/components/productWorkspaceStoryboard.test.ts src/components/productWorkspaceTasks.test.ts src/components/ProductWorkspaceStoryboardSection.test.tsx`
- `npm run build`

> 备注：`npm` 会提示 `--run` 参数未来版本兼容性问题，但本次 `vitest` 已实际执行并通过。

## 产物

截图：

- `artifacts/acceptance-5177-task-center-post-copy-update-v2.png`
- `artifacts/acceptance-5177-storyboard-post-copy-update-v2.png`

相关源码：

- `web/src/components/productWorkspaceStoryboard.ts`
- `web/src/components/productWorkspaceStoryboard.test.ts`

## 结论

本轮验收通过。

任务中心关于“推荐恢复 / 人工修复”的文案语义已经在真实页面上成立，镜头工作台的 `待恢复` 乱码也已经修复。当前剩余问题不在文案层，而在更深一层的提示词质量与关键资产继承策略，需要继续按蓝图推进到提示词编译和资产引用治理阶段。
