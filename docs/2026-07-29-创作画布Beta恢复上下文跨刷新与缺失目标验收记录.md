# 2026-07-29 创作画布 Beta 恢复上下文跨刷新与缺失目标验收记录

## 本轮目标

继续把 `创作画布 Beta` 的恢复链路往生产态推进：

1. 恢复上下文不应在刷新后丢失
2. `target missing` 场景要能稳定落地到真实页面状态
3. `missing` 场景下的按钮与文案要符合之前设计

## 本轮实现

涉及文件：

- `D:\Work\Project\screenplay-agent-refactor-v2\web\src\components\productWorkspaceNavigationState.ts`
- `D:\Work\Project\screenplay-agent-refactor-v2\web\src\components\productWorkspaceNavigationState.test.ts`
- `D:\Work\Project\screenplay-agent-refactor-v2\web\src\components\ProductWorkspace.tsx`

新增能力：

1. 新增 `productWorkspaceNavigationState.ts`
   - 负责读写正式工作台里的 canvas 恢复上下文
   - 当前仅持久化：
     - `section: 'canvas'`
     - `canvasNavigationTarget`
2. 新增清理规则：
   - 若离开 `canvas`
   - 或当前已无有效 `canvasNavigationTarget`
   - 则自动移除本地持久化状态
3. 恢复规则：
   - 打开项目后，若本地存在合法 canvas 恢复上下文
   - 工作台会直接恢复到 `创作画布 Beta`
   - 并继续沿用原来的恢复目标

## 测试与构建

执行：

```bash
cd web
npm run test -- productWorkspaceCanvasBeta productWorkspaceTaskCenterSelectedTaskPanel productWorkspaceNavigationState
npm run build
```

结果：

- 3 个测试文件通过
- 18 个测试通过
- 构建通过

## 真浏览器验收

验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 项目：`三个和尚`

### 一、跨刷新恢复验收

路径：

`三个和尚 -> 任务中心 -> 首帧结果回收 -> 在创作画布查看 -> 刷新页面`

结果：

```json
{
  "beforeReloadHasRecovery": true,
  "afterReloadHasRecovery": true,
  "afterReloadHasCanvas": true
}
```

说明：

- 刷新前存在恢复上下文
- 刷新后仍停留在 `创作画布 Beta`
- 恢复上下文未丢失

### 二、缺失目标验收

模拟方式：

1. 在浏览器中写入：
   - `product-workspace.navigation-state.14`
2. 伪造不存在的恢复目标：
   - `episode: 1`
   - `shotId: 999`
   - `taskId: missing-task-001`
   - `recoveryKind: frame`
3. 刷新页面并等待同步态自然结束

结果：

```json
{
  "hasSyncing": false,
  "hasRecovery": true,
  "hasTargetMissing": true,
  "hasClosureMissing": true,
  "hasRefocus": false,
  "hasBackTasks": true,
  "hasStoryboard": true
}
```

页面确认到：

1. `恢复上下文`
2. `暂未定位到恢复目标`
3. `恢复收口待人工确认`
4. 说明文案：
   - `当前更适合先回任务中心或业务工作台确认最新链路，再决定是否继续在画布里推进。`
5. 按钮行为：
   - `定位恢复节点` 不出现
   - `回任务中心` 出现
   - `前往镜头工作台` 出现

### 三、环境恢复确认

验收完成后已清理：

- `product-workspace.navigation-state.14`

回归检查：

```json
{
  "hasMissing": false,
  "hasProjectList": true
}
```

说明：

- 本次模拟缺失目标状态未残留到本地环境

## 结论

- 通过

这一步的价值在于：

1. 恢复上下文已经具备跨刷新连续性
2. `target missing` 不再只是逻辑函数里的理论状态，而是可在真浏览器中稳定观察到的正式界面状态
3. 这让 `创作画布 Beta` 更接近可持续创作的实际工作面
