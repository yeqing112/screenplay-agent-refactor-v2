# 2026-07-20 阶段任务补充

## 已完成

- 修复项目列表页乱码文案，恢复项目状态、按钮、空态、加载态、删除确认提示的正常中文显示。
- 修复项目标题降级逻辑，损坏标题现在会稳定回退为 `未命名项目 {id}`。
- 修复旧版交付记录状态与统计字段的可见乱码。
- 重启本地联调环境后，前端已正确指向 `http://127.0.0.1:18765`，不再错误显示 `http://localhost:8765`。
- 真浏览器复验通过 `三个和尚 -> 正式工作台 -> 资产中心` 主路径。
- 项目控制台的“当前下一步”已改为真实可点击入口，可直接跳到推荐修复模块。

## 对应验收标准

1. 项目列表页必须显示正确的 API Proxy 目标。
2. 项目列表页不得再出现显著中文乱码。
3. `三个和尚 -> 正式工作台 -> 资产中心` 路径必须可在真浏览器中走通。
4. 资产中心右侧详情、参考图管理、影响范围三个区域必须同时可见。
5. `预览`、`删除`、`查看来源`、`重生新版参考图` 等关键动作入口必须存在。
6. 项目控制台中的主建议动作不能只是静态提示，必须能跳转到对应正式模块。

## 验收结果

- `npm run build` 通过。
- `npm test -- ProductWorkspaceAssetsSection.test.tsx` 通过。
- 浏览器验证：
  - 首页显示 `API Proxy: http://127.0.0.1:18765`
  - 首页中文文案恢复正常
  - `三个和尚 -> 正式工作台 -> 资产中心` 可正常进入
  - 资产中心关键动作入口可见
  - 文本检查未发现新的 `???` 型乱码
  - `三个和尚 -> 正式工作台 -> 项目控制台 -> 立即前往` 可真实跳转到 `改编方向`

## 后续优先级

1. 继续清理正式工作台其余用户可见乱码与历史编码污染。
2. 按蓝图推进数据完整性问题，优先补齐资产与镜头的真实闭环。
3. 回到提示词质量整改任务，继续增强人物/场景/道具版本化提示词的稳定性与引用能力。
