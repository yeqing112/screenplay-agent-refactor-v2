# 2026-07-27 任务中心最新状态重生成语义补验记录

## 验收目标

- 任务中心需要明确区分三类恢复链路：
  - 重编后继续生成
  - 按原始输入重发
  - 按最新镜头状态重生成
- 任务详情中的“提示词版本关系说明”不能再用泛化文案混淆“最新状态重生成”和“普通重发”。
- 页面文案不能出现乱码或问号占位。

## 本次调整

- 文件：`web/src/components/productWorkspaceTaskCenterSelectedTaskPanel.tsx`
- 调整点：
  - 为 `task_center_regenerate_latest_video`
  - 为 `task_center_regenerate_latest_frame`
  - 单独输出“直接使用镜头当前状态发起”的版本说明
- 同时修复该说明区块的历史编码污染，消除页面中的 `???` 文案。

## 预期行为

- 如果任务来自“按最新状态重生成”链路，详情中应显示：
  - `本次任务直接使用镜头当前状态发起，实际提交给模型的是当前镜头版本 vX。`
- 如果任务来自“重编后继续生成”链路，详情中应显示：
  - `本次任务由重编版本 vY 触发，但真正提交给模型的是后续演进到的 vX。`
- 如果任务只是普通任务且无重编版本，应显示：
  - `本次任务实际提交给模型的是 vX。`

## 自动化验证

- `npx vitest run src/components/productWorkspaceTaskCenterData.test.ts src/components/productWorkspaceTaskCenterDetailPanels.test.tsx src/components/productWorkspaceTaskRecoveryActions.test.ts`
  - 结果：通过
- `npm run build`
  - 结果：通过

## 真浏览器验收

### 环境

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765](http://127.0.0.1:18765)

### 验收路径

1. 打开正式产品工作台
2. 进入项目 `三个和尚`
3. 打开 `任务中心`
4. 选中任务 `视频结果回收`

### 页面实际结果

- 任务详情已显示：
  - `触发链路：重编后继续生成视频`
  - `任务提示词版本：v19`
  - `镜头当前版本：v19`
  - `版本差异：与当前镜头版本一致`
  - `重编版本：v12`
  - `本次任务由重编版本 v12 触发，但真正提交给模型的是后续演进到的 v19。`
- 页面中原先的 `???` 文案已消失。

## 验收结论

- 通过。
- 任务中心现在已经能把“链路来源”和“实际提交版本”拆开说明，用户不会再把“重编触发”和“最新状态提交”混为一谈。

## 后续建议

- 下一步应补一条真实的“按最新状态重生成”样本任务，避免这条链路只停留在按钮和文案层验收。
- 随后可以继续把任务中心的“恢复链路说明”同步到镜头工作台提交前确认区，做到前后语义一致。
