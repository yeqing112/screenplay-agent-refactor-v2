# 2026-07-21 改编方向锁定链路验收记录

## 验收目标

验证正式工作台中的 `改编方向 -> 锁定主方向 -> 下游状态传播` 是否真实可用，而不是仅停留在本地展示状态。

## 验收环境

- 日期：2026-07-21
- 前端：`http://127.0.0.1:5175`
- 后端：`http://127.0.0.1:18765`
- 验收方式：Playwright 真浏览器操作
- 验收项目：`三个和尚`

## 实际操作路径

1. 打开首页，进入项目 `三个和尚`
2. 进入 `正式工作台`
3. 在 `项目控制台` 确认当前阻塞为 `改编方向 待补锁`
4. 进入 `改编方向`
5. 验证候选卡存在，且 `锁定为主方向` 初始为禁用态
6. 点击候选方向 `竖屏情绪悬疑短剧`
7. 再次验证 `锁定为主方向` 变为可点击
8. 点击 `锁定为主方向`
9. 依次进入：
   - `项目控制台`
   - `任务中心`
   - `导出中心`
   - `剧本工作台`
   - `镜头工作台`
10. 检查锁定状态是否传导

## 真实浏览器结论

### 1. 锁定动作本身可用

- 按钮并非缺失
- 初始不可点的真实原因是：`尚未选择候选方向`
- 选择候选方向后，按钮可点击
- 点击后，项目主方向成功锁定

### 2. 下游状态传播已打通

#### 项目控制台

- `改编方向` 从 `待补锁` 变为 `已锁定`
- 显示 `当前主方向：竖屏情绪悬疑短剧`
- `当前下一步` 不再要求先锁改编方向，改为建议处理 QA 阻塞

#### 任务中心

- `改编方向锁定` 任务状态变为 `已完成`
- 不再作为上游阻塞项占据默认焦点
- 当前真实待推进任务切换为 `资产补全` 和 `QA 修复`

#### 导出中心

- `项目改编方向未锁定` 阻塞已消失
- 当前阻塞转为真实交付问题：
  - 缺已采纳分镜图
  - 缺已采纳视频
  - QA 问题未清零

#### 剧本工作台

- 不再提示先锁定项目改编方向
- 当前剧本状态转为真实问题视角：`脚本 QA 未清`

#### 镜头工作台

- 不再处于改编方向只读阻塞
- 当前阻塞转为真实镜头生产问题：
  - 缺参考资产
  - 缺分镜图版本
  - 缺视频版本

## 发现的问题

### P1 交互理解成本偏高

虽然链路已通，但真实用户第一次进入时，容易误解为“锁定功能坏了”，因为：

- 锁定按钮先显示但不可点击
- 候选方向“已选中”的反馈不够强
- 锁定按钮附近缺少明确提示，说明为什么当前不可锁

## 本轮修复动作

已对 `ProductWorkspaceAdaptationSection` 做交互补强：

- 强化候选卡选中态
- 增加“已选中候选方向”提示
- 增加锁定前条件提示
- 明确锁定后对剧本 / 分镜 / 资产策略的继承关系
- 补充对应组件测试

## 相关证据

- `artifacts/acceptance-2026-07-21-home-before-lock.png`
- `artifacts/acceptance-2026-07-21-adaptation-before-lock.png`
- `artifacts/acceptance-2026-07-21-adaptation-selected-candidate.png`
- `artifacts/acceptance-2026-07-21-adaptation-locked-final.png`
- `artifacts/acceptance-2026-07-21-项目控制台-locked-check.png`
- `artifacts/acceptance-2026-07-21-任务中心-locked-check.png`
- `artifacts/acceptance-2026-07-21-导出中心-locked-check.png`
- `artifacts/acceptance-2026-07-21-剧本工作台-locked-check.png`
- `artifacts/acceptance-2026-07-21-镜头工作台-locked-check.png`

## 验收结论

本项验收通过。

通过的不是“页面上有一个锁定按钮”，而是：

1. 真浏览器里可以完成候选选择与主方向锁定
2. 锁定状态会真实传导到项目控制台、任务中心、剧本、分镜和导出中心
3. 下游阻塞会从“缺项目级锁定”切换成更真实的生产问题

下一步应继续推进：

- 资产补全闭环
- QA 修复动作闭环
- 镜头静态/运动提示词质量整改
- 分镜图与视频采纳率提升链路
