# 2026-07-25 资产中心筛选与动作文案验收记录

## 1. 本轮目标

继续按《产品重构蓝图》和《产品重构阶段任务与验收标准》推进资产中心收口，补两类容易影响真实用户体验的问题：

1. 资产中心分类筛选需要有自动化保护，避免后续改动把 `全部 / 人物 / 场景 / 道具` 这组正式入口改坏。
2. 资产动作链里的用户可见提示文案仍有历史乱码残留，一旦用户执行生成、删除、状态切换、保存镜头绑定，就可能直接看到异常文案。

## 2. 本轮改动

### 2.1 资产动作层文案清理

修复文件：

- `web/src/components/productWorkspaceAssetActions.ts`

处理内容：

- 统一清理以下动作的用户可见提示文案：
  - 生成参考图
  - 删除参考图
  - 更新参考图状态
  - 保存镜头绑定
- 保持原有动作逻辑不变，只收口用户提示层。
- 采用稳定的 Unicode 转义字符串，避免终端编码环境影响源文件可用性。

### 2.2 资产中心分类筛选测试补齐

修复文件：

- `web/src/components/ProductWorkspaceAssetsSection.test.tsx`

新增保护：

- 验证页面中存在正式分类筛选入口：
  - `全部`
  - `人物`
  - `场景`
  - `道具`
- 验证当前筛选摘要仍正常输出，不出现空白或异常替换。

## 3. 自动化验收

执行命令：

```bash
npm --prefix web run test -- ProductWorkspaceAssetsSection.test.tsx productWorkspaceAssetActions.test.ts productWorkspaceTaskCenterData.test.ts productWorkspaceTaskRecoveryActions.test.ts
```

结果：

- `4` 个测试文件通过
- `17` 条测试全部通过

覆盖重点：

1. 资产中心详情区与参考图动作入口仍正常渲染
2. 资产中心分类筛选入口受测试保护
3. 资产参考图回写目标推导逻辑未回退
4. 任务中心提示词恢复链路未被本轮改动破坏

## 4. 真浏览器验收

验收时间：

- 2026-07-25

验收地址：

- 前端：`http://127.0.0.1:5182`

验收路径：

1. 打开首页
2. 进入项目 `三个和尚`
3. 点击 `正式工作台`
4. 切换到 `资产中心`
5. 检查分类筛选与详情区动作入口

验收结果：

1. 分类筛选按钮全部可见：
   - `全部`
   - `人物`
   - `场景`
   - `道具`
2. 点击 `人物` 后，筛选摘要已正确显示 `类型“人物”`
3. 参考图动作入口可见：
   - `预览`
   - `查看来源`
   - `删除`
4. 详情区标题 `上下游交接状态` 正常显示
5. 当前页面未发现新的 `?????` 式乱码回归

验收截图：

- `artifacts/acceptance-2026-07-25-asset-center-filters-and-actions.png`

## 5. 结论

本轮已经完成以下收口：

1. 资产中心分类筛选从“页面可用”升级为“页面可用 + 自动化保护”
2. 资产动作链用户提示从“可能触发乱码”升级为“正式中文文案”
3. 任务中心相关回收链路在本轮改动后仍保持稳定

## 6. 下一步建议

下一步继续按蓝图推进更高价值的全局能力，不再停留在文案层：

1. 继续补资产中心和镜头工作台之间的真实闭环验证
2. 补导出中心的最新验收与交付包一致性检查
3. 继续推进提示词编译质量治理，尤其是静态资产引用、角色多变体继承和分镜状态注入
