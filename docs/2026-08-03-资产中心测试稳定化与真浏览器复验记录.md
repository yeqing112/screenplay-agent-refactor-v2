# 2026-08-03 资产中心测试稳定化与真浏览器复验记录

更新时间：2026-08-03（周一）

## 1. 目标

本轮目标是把资产中心相关测试从历史乱码样本中解耦出来，并确认资产中心主视图在真实浏览器下仍保持正常中文展示。

## 2. 本轮处理范围

### 2.1 测试重建

重建并清洗以下测试文件：

- `web/src/components/ProductWorkspaceAssetsSection.test.tsx`
- `web/src/components/productWorkspaceAssetViewController.test.ts`

处理内容：

- 将历史乱码样本统一替换为正常中文测试数据
- 将历史乱码断言替换为真实产品文案断言
- 保留原有测试场景覆盖，包括：
  - 分类筛选
  - 来源提示词查看
  - 缺参考图承接
  - 旧提示词重生
  - QA 承接
  - 待同步镜头绑定
  - 分镜精调恢复
  - 任务中心回收
  - 创作画布承接
  - 运行态摘要

### 2.2 真实页面复验

对正式产品工作台中的资产中心主视图做真浏览器复验，重点检查：

- 分类筛选文案
- 状态筛选文案
- 版本筛选文案
- 资产卡片区文案
- 待同步绑定、重生新版参考图、补参考图等高频动作文案

## 3. 自动化回归

执行命令：

```bash
cd web
npm run test -- ProductWorkspaceAssetsSection productWorkspaceAssetViewController
npm run build
```

结果：

- `ProductWorkspaceAssetsSection` 相关测试通过
- `productWorkspaceAssetViewController` 相关测试通过
- `vite build` 通过

## 4. 真实浏览器验收

验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765](http://127.0.0.1:18765)
- 项目：`三个和尚`（`bookId = 14`）

验收路径：

1. 首页
2. 进入 `三个和尚`
3. 打开正式产品工作台
4. 打开 `资产中心`

### 验收结果

资产中心主视图关键区域均为正常中文：

- `全部 / 人物 / 场景 / 道具`
- `全部状态 / 缺参考图 / 有图待选 / 提示词已过期 / 已有锁定图`
- `全部版本 / 基础定妆 / 分集默认 / 分镜精调 / 场景多版本 / 道具多版本`
- `待同步绑定`
- `重生新版参考图`
- `补参考图`
- `查看资产`

同时确认：

- 页面未出现 `瀵`、`閫`、`鍐呭`、`Bible` 等历史脏文本
- 人物、场景、道具卡片均能正常展示
- 分镜精调与分集默认版本标签显示正常

## 5. 验收结论

本轮资产中心测试稳定化与真浏览器复验通过。

已经完成：

- 资产中心相关测试从历史乱码断言中脱钩
- 资产中心主视图高频文案在真实浏览器下确认正常
- 测试、构建、真实页面三者结果一致

## 6. 后续建议

1. 继续清理资产中心之外的剩余历史乱码测试文件
2. 后续如调整资产中心文案，以正常中文为唯一测试基线
3. 继续推进正式工作台其他模块的高频真浏览器巡检
