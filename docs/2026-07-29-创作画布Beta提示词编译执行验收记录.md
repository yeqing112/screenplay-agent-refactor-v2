# 2026-07-29 创作画布 Beta 提示词编译执行验收记录

## 验收目标

验证创作画布 Beta 已补齐 L2 第二个关键动作：

- 在画布中选中单个分镜节点
- 直接发起提示词重编译任务
- 进入真实异步编译任务轮询链路
- 编译完成后刷新工作区数据
- 若任务未即时完成，可继续回到任务中心恢复

## 本次实现

涉及文件：

- `web/src/components/ProductWorkspaceCanvasBetaSection.tsx`

已新增能力：

1. 创作画布 `分镜` 节点详情中的 `单镜头执行` 卡片新增：
   - `重编译提示词`
   - `去任务中心继续回收提示词`
2. 调用真实接口：
   - `POST /api/books/{bookId}/storyboard/{episode}/{shotId}/compile-prompts/async`
3. 接入真实任务轮询：
   - `GET /api/storyboard-prompt-compile-tasks/{taskId}`
4. 接入本地恢复任务记录：
   - `kind: prompt`
5. 编译完成后刷新工作区数据

## 构建与测试

- `npm run build` 通过
- `npm run test -- productWorkspaceCanvasBeta productWorkspaceTaskCenterSelectedTaskPanel` 通过

## 真浏览器验收

验收地址：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)

验收方式：

- 使用真实浏览器打开正式工作台
- 使用前端真实点击路径
- 对提示词编译接口和任务状态接口做浏览器侧网络拦截，避免实际触发模型调用

验收步骤：

1. 打开 `三个和尚`
2. 进入 `正式工作台 -> 创作画布 Beta`
3. 在右侧结果列表中选中 `分镜阻塞镜头 4`
4. 在节点详情区确认出现 `重编译提示词`
5. 点击 `重编译提示词`
6. 观察请求、轮询与成功文案

浏览器观测结果：

- 已出现 `重编译提示词` 按钮
- 实际发起请求：
  - `http://127.0.0.1:5175/api/books/14/storyboard/1/4/compile-prompts/async`
- 实际进入任务轮询：
  - `http://127.0.0.1:5175/api/storyboard-prompt-compile-tasks/canvas-prompt-001`
- 页面出现成功文案：
  - `已重新编译到 v12。`
- 页面保留恢复入口：
  - `去任务中心继续回收提示词`

## 验收结论

创作画布 Beta 已完成 L2 第二个执行动作的前端接入：

- 不再只能从画布生成首帧
- 已可从画布直接发起单镜头提示词重编译
- 已接入真实异步编译恢复语义

## 下一步建议

1. 在创作画布继续接入 `生成视频`
2. 增加 `重编译提示词后继续生成首帧` 组合动作
3. 将提示词版本号、更细的恢复状态回写到画布节点摘要
