# 2026-07-21 分镜编译运行态一致性整改记录

## 本轮目标

收口镜头工作台里“结构化绑定参考图”和“编译器实际使用参考图”长期漂移的问题，让后端在两条链路上遵循同一套运行时语义：

1. 新编译时，`used_assets` 决定本次真正进入模型的参考图载荷。
2. 旧数据读取时，`prompt_compiler` 元信息能按当前绑定关系自动自愈，而不是继续沿用过期的 `reference_images / reference_asset_ids / compiler_warnings`。

## 已完成整改

### 1. 旧 `prompt_compiler` 元信息改为按运行态整体重建

调整文件：

- [api/server.py](D:/Work/Project/screenplay-agent-refactor-v2/api/server.py)

关键变化：

- ` _refresh_legacy_prompt_compile_meta(...) ` 不再只刷新 `prompt_compile_context` 的一部分字段。
- 新增运行态重建逻辑，对以下字段统一重算：
  - `prompt_compile_context.compiled_reference_images`
  - `prompt_compile_context.compiled_reference_asset_ids`
  - `used_assets`
  - `reference_images`
  - `reference_asset_ids`
  - `compiler_warnings`
  - `compiler_diagnostics`

### 2. 编译器诊断改为优先基于“实际编译参考图载荷”

调整文件：

- [api/server.py](D:/Work/Project/screenplay-agent-refactor-v2/api/server.py)

关键变化：

- ` _build_prompt_compiler_diagnostics(...) ` 现在优先读取：
  - `compiled_reference_images`
- 只有在缺失时才回退到：
  - `reference_images`

这让诊断里的：

- `reference_image_count`
- `reference_image_payload`

都与“当前真实编译使用”保持一致，不再混用“所有绑定参考图总表”。

### 3. 新编译结果的顶层输出也改为回写“实际编译使用”

调整文件：

- [api/server.py](D:/Work/Project/screenplay-agent-refactor-v2/api/server.py)

关键变化：

- ` _compile_storyboard_prompts(...) ` 在拿到 LLM 返回的 `used_assets` 之后，会计算本次有效参考图载荷。
- 编译结果顶层返回的：
  - `reference_images`
  - `reference_asset_ids`

现在回写的是“本次真正进入模型的参考图”，不再直接使用 compile context 中“所有可绑定参考图总表”。

### 4. 补充后端回归测试

调整文件：

- [tests/test_storyboard_prompt_context_legacy_refresh.py](D:/Work/Project/screenplay-agent-refactor-v2/tests/test_storyboard_prompt_context_legacy_refresh.py)

新增验证点：

- 旧 `prompt_compiler.used_assets` 中已命中的人物资产，若当前已经有正式参考图，刷新后必须恢复为 `has_reference = true`。
- `compiled_reference_asset_ids` 必须由“当前实际使用且有图”的资产重建，而不是沿用历史假 ID。
- 旧告警不能继续把已经有图的人物错误报成“缺参考图”。

## 本轮验收

### 自动化验收

后端：

```powershell
python -m unittest tests.test_storyboard_prompt_context_legacy_refresh tests.test_storyboard_prompt_authority_context tests.test_storyboard_generation_flow
```

结果：

- 3 组测试全部通过

前端：

```powershell
cd web
npm test -- ProductWorkspaceStoryboardSection.test.tsx productWorkspacePrompt.test.ts productWorkspaceAssets.test.ts productWorkspaceAssetViewController.test.ts
npm run build
```

结果：

- `29 passed`
- 构建通过

### 运行态复核结论

2026-07-21 本地固定验收端口 `18766 / 4177` 上仍挂着旧进程，接口返回仍停留在整改前语义：

- `compiled_reference_asset_ids = ["ref-8", "ref-34"]`
- `used_assets` 里 `和尚乙 / 和尚丙` 仍显示 `has_reference = false`
- `compiler_warnings` 仍错误提示这两个角色缺参考图

这说明：

- 旧运行进程尚未加载本轮代码
- 当前浏览器页面不能作为本轮整改结果的最终运行态证据

## 补充的工程支持

为减少后续真页面验收反复命中旧进程，本轮顺手补强了本地启动脚本：

- [scripts/start-local-dev.ps1](D:/Work/Project/screenplay-agent-refactor-v2/scripts/start-local-dev.ps1)
- [docs/local-dev-ports.md](D:/Work/Project/screenplay-agent-refactor-v2/docs/local-dev-ports.md)

新增能力：

- 支持自定义 `BackendPort`
- 支持自定义 `FrontendPort`
- 支持选择 `FrontendMode = dev | preview`
- `preview` 模式会先构建，再起稳定预览服务
- 启动前会提示端口是否已被占用

## 下一步验收标准

在重新启动到一组全新端口后，以下结果必须同时成立，才算本问题彻底收口：

1. `GET /api/pipeline/book/14/outputs` 中，镜头 `1` 的 `used_assets` 不再把当前已有参考图的人物标为 `has_reference = false`。
2. `compiled_reference_asset_ids` 与 `used_assets` 中“真正有图且被使用”的资产一致。
3. `compiler_warnings` 不再错误提示 `和尚乙 / 和尚丙` 缺参考图。
4. 镜头工作台页面里：
   - `参考图输入`
   - `当前编译实际使用`
   - `编译告警`

   三处信息能相互对齐。
5. 单镜头视频生成请求和任务回写 metadata 中，继续保持：
   - `firstFrameAssetId`
   - `referenceAssetIds`
   - `referenceImages`

   这三组信息可追溯、可解释。
