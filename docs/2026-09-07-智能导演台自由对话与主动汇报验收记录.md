# 智能导演台自由对话与主动汇报验收记录

日期：2026-09-07  
阶段：Phase 1 第一批

## 已完成

- 新增 `AgentProjectUpdate` 模型和 Alembic 迁移 `c5e6f7a8b9c0`。
- 新增服务端事实驱动的项目动态推导器，覆盖项目进度、QA 问题、任务失败/进行中、资产未就绪。
- 新增主动动态接口：
  - `POST /api/agent/updates/reconcile`
  - `GET /api/agent/updates`
  - `GET /api/agent/updates/summary`
  - `POST /api/agent/updates/{id}/state`
- 相同 `dedupe_key + evidence_fingerprint` 重复检查会复用已有动态，不产生重复通知。
- 新增自由对话接口 `POST /api/agent/chat`：无 Agent 模型时给出确定性项目摘要；有模型时自动进行只读对话；模型返回副作用时只保存动作提案，不执行生产操作。
- 智能导演台抽屉接入项目动态、未读徽标和后台周期检查；任务中心时间线可显示项目动态。
- 普通对话不再调用旧的“预览 LLM → 确认调用”流程。

## 自动化验证

- 后端全量：`515 passed`。
- Agent/主动汇报相关：自由对话、项目动态、附件、草案共 `13 passed`。
- 前端 Agent/任务中心相关：`6 passed`。
- 前端生产构建：通过。

## 当前边界

- 当前主动检查采用“抽屉打开时 30 秒、关闭时 120 秒”的轮询；SSE 推送留在下一批。
- 副作用动作目前只返回结构化提案，尚未在 Agent 抽屉内直接执行；执行仍需承接既有镜头/资产/QA 工作台门禁。
- LLM 调用失败时会回退确定性摘要并记录审计，不会自动重试产生额外费用。

### 2026-09-08 承接闭环补充

- 对话产生的副作用动作提案现在带有可追溯 `audit_id`。
- 用户点击“确认并打开工作台”时，服务端只将审计状态记为 `handoff_confirmed`，并返回目标工作台与镜头/资产上下文；不执行写入、生成或付费调用。
- 正式工作台通过统一 `smart-director:navigate` 事件承接，继续复用原有证据、锁定、版本、预算和最终确认门禁。
- “稍后处理”只清除当前对话卡片，不改变项目事实或生产状态。

### 2026-09-08 真实浏览器验证

- 发现并修复本地前端旧进程运行目录错误导致 `5175` 返回 404 的问题；正确重启后页面返回 `200`。
- 使用独立 Playwright Chromium 完成真实页面链路：项目列表 → 打开项目 `990309` → 正式工作台 → 智能导演台 → 项目动态/未读徽标 → 普通对话回复。
- 页面控制台错误数为 0；截图证据：`artifacts/real-browser-agent-drawer.png`、`artifacts/real-browser-agent-task-center.png`。
- 桌面浏览器桥接仍会对回环地址报 `ERR_BLOCKED_BY_CLIENT` 或超时；已采用独立浏览器进程作为稳定验收通道。
