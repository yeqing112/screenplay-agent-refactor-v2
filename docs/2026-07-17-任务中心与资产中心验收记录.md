# 2026-07-17 任务中心与资产中心验收记录

## 1. 任务中心恢复链路持久化

### 本次完成
- 后端创意任务状态新增并持久化：
  - `restarted_from_task_id`
  - `restart_count`
  - `last_restarted_at`
- 前端任务中心恢复条目改为优先读取服务端恢复元数据，不再只依赖浏览器本地暂存。
- 针对恢复链路补齐前后端自动化覆盖。

### 验收命令
- 后端回归：
  - `python -m unittest tests.test_poyo_creative_task_state -q`
- 前端单测：
  - `npm test -- productWorkspaceTaskCenterData.test.ts productWorkspaceTaskRecoveryActions.test.ts productWorkspaceTasks.test.ts`
- 前端构建：
  - `npm run build`

### 真浏览器验收
- 访问：
  - `http://127.0.0.1:5175`
- 路径：
  - `三个和尚 -> 正式工作台 -> 任务中心`
- 真实验收任务链路：
  - 失败任务：`2a7bb0d6bf09`
  - 重发成功任务：`d0c766a2c730`
- 验收结果：
  - 刷新前显示：
    - `任务号：d0c766a2c730`
    - `来源任务：2a7bb0d6bf09`
    - `重发次数：1`
    - `最近重发：2026/7/17 05:03:17`
  - 刷新页面后上述 4 项仍保留。

### 结论
- 任务中心恢复链路已从“本地暂存可见”提升为“服务端状态可恢复”。
- 满足蓝图中“真实创意任务恢复链路”与“真浏览器验收”要求。

---

## 2. 资产中心多维筛选巩固

### 本次完成
- 将资产中心筛选计算抽离为纯函数：
  - `web/src/components/productWorkspaceAssetViewController.ts`
  - `buildAssetFilterState(...)`
- 新增自动化测试覆盖：
  - 类型筛选
  - 状态筛选
  - 版本筛选
  - 集数/搜索范围
  - 选中资产保留行为

### 验收命令
- 前端单测：
  - `npm test -- productWorkspaceAssetViewController.test.ts ProductWorkspaceAssetsSection.test.tsx`
- 前端构建：
  - `npm run build`

### 真浏览器验收
- 访问：
  - `http://127.0.0.1:5175`
- 路径：
  - `三个和尚 -> 正式工作台 -> 资产中心`
- 实际交互顺序：
  - 初始：`全部`
  - 切换：`人物`
  - 再切换：`缺参考图`
  - 再切换：`道具`
- 页面摘要变化：
  - 初始：`筛出 42 项`
  - 人物：`筛出 3 项`
  - 人物 + 缺参考图：`筛出 1 项`
  - 道具 + 缺参考图：`筛出 9 项`

### 结论
- 资产中心“人物 / 场景 / 道具 + 状态 + 版本”多维筛选在真实页面中已可用。
- 自动化测试与浏览器交互验收均已补齐。

---

## 3. 当前判断

### 已确认稳定
- 任务中心真实恢复链路
- 任务重发历史刷新后保留
- 资产中心多维筛选
- 相关前端构建与单测

### 下一步建议
- 继续推进分镜提示词与人物定妆提示词链路的产品化验收。
- 优先检查“手动切换资产筛选时，详情面板是否应保留上一个选中资产”的交互策略，再决定是否做场景化调整。

## 6. �־���ʾ��Ȩ��ԴժҪ�İ����գ�2026-07-17��

### 6.1 ��������

- ���� `productWorkspacePrompt.ts` �������ʲ� `referenceSourceLabel` ���ж��߼���
- ��ɫ����Դ������ͳ��ʾΪ����ǰ�����汾�������ǰ�ʵ�ʱ��巶Χ���֣�
  - `episode_default` -> `��ǰ�ּ�Ĭ�϶�ױ`
  - `shot_variant` -> `��ǰ������ױ`
- ͬ������ǰ�˵��⣬�����������İ��ع顣

### 6.2 �������������

- ͨ����
  - `npm test -- productWorkspacePrompt.test.ts productWorkspaceStoryboardBindings.test.ts`
  - `npm run build`

### 6.3 �����������

- ·����
  - `http://127.0.0.1:5175/`
  - `�������� -> ��ͷ����̨`
- ʵ��ҳ��������
  - `����Ȩ��ԴժҪ` ����ɼ�
  - �ּ�Ĭ��������Ŀ��ʾ `�ο���Դ����ǰ�ּ�Ĭ�϶�ױ`
  - ����������Ŀ��ʾ `�ο���Դ����ǰ������ױ`
  - ҳ����ͬʱ�ܿ��� `��Ч�汾���ּ�Ĭ�� / ��1��Ĭ������`

### 6.4 ���ս���

- �־���ʾ��Ȩ��ԴժҪ�İ��Ѻ�ʵ�ʱ���������룬���ٰѡ��ּ�Ĭ�϶�ױ�����ɡ���ǰ�����汾����
- ����������ͼ�С���ʾ�ʱ���ɶ�����׷�١���������������ˡ��Ľ׶�Ҫ��

### 6.5 ���β������գ���ͷ��ժҪ��������

- �������� `productWorkspaceStoryboardBindings.ts` �� `active_variant` ���İ��߼���
- ֮ǰ�÷�֧δ���� `episode_default` �� `shot_variant`�����¾�ͷ����ϸ����֣�
  - `��ǰ���壺��1��Ĭ������`
  - `�ο���Դ����ǰ������ױ`
  ������������䡣
- ������
  - `active_variant + episode_default` -> `��ǰ�ּ�Ĭ�϶�ױ`
  - `active_variant + shot_variant` -> `��ǰ������ױ`

### 6.6 ���λع��������������

- ͨ����
  - `npm test -- productWorkspaceStoryboardBindings.test.ts productWorkspacePrompt.test.ts productionModeMakeup.test.ts`
  - `npm run build`
- �����������·����
  - `http://127.0.0.1:5175/`
  - `�������� -> ��ͷ����̨`
- ��������
  - ҳ���Ѳ��ٳ��� `��1��Ĭ������ �� �ּ�Ĭ�� �� ��ǰ������ױ`
  - ҳ�����ȶ���ʾ `��1��Ĭ������ �� �ּ�Ĭ�� �� ��ǰ�ּ�Ĭ�϶�ױ`
  - `����Ȩ��ԴժҪ` �뾵ͷ����ϸ����������һ��

### 6.7 �°澵ͷ����̨����ͷ������ڲ��루2026-07-17��

- �°� `��ͷ����̨` �Ѳ��ϵ���ͷ `������֡` / `������Ƶ` ��ڣ�����ֻ����ʾ�����ʲ�����ҳ��
- ��Ƶ��ť����ǰ��Լ����
  - û���Ѳ�����֡ʱ����
  - �������� / ����δ����ʱ����
- ����ͷ���ɻ����ʵ����д�����лָ���·��
  - ���� pending ����Ǽ�
  - `waitForCreativeTask(...)` ��ѯ�� reconcile
  - ������ɼ���ת���������Ļָ�
- ��ͷ����ҳ���� `ȥ�������ļ�������` ��ڣ����ڴӵ���ͷֱ����ת����������׷�ٳ�����

### 6.8 ���ֻع������������

- ͨ����
  - `npm test -- ProductWorkspaceStoryboardSection.test.tsx productWorkspacePrompt.test.ts productWorkspaceStoryboardBindings.test.ts productionModeMakeup.test.ts`
  - `npm run build`
- �����������·����
  - `http://127.0.0.1:5175/`
  - `�������� -> ��ͷ����̨`
- ��ҳ������
  - ���Կ��� `����ͷ���ɶ���` ģ��
  - ���Կ��� `������֡`��`������Ƶ`��`ȥ�������ļ�������` �������
  - ��ǰ��Ŀ�����£�����δ��ȫ����ʱ����֡����Ƶ��ť��һ����ã�����ʾ��
    - `��ǰ��ͷ������������/����Լ�����ݲ�����ֱ�ӳ�ͼ�����Ƶ��`
- ˵����
  - `���Ѳ�����֡ -> ��Ƶ��ť����`
  - `���в�����֡ -> ��Ƶ������`
  ������ǰ��״̬���� `ProductWorkspaceStoryboardSection.test.tsx` ���ǡ�

## 7. QA 修复工作台后台复检回流验收（2026-07-17）

### 7.1 本轮补齐

- ProductWorkspaceQaSection.tsx 已补齐真实 QA 工单动作后的后台复检回流闭环：
  - 自动修复 返回 echeck.status=running 时，前端会持续刷新 /api/books/{bookId}/qa/workbench
  - 触发复检 返回 status=running 时，前端会持续刷新真实 QA 工作台
  - 轮询结束后，页面会自动回写最终结论，不再要求用户手动刷新确认
- 新增前端辅助单测，覆盖：
  - 定位真实 workbench issue
  - 统计分集未通过 issue 数量
  - 复检通过 / 未通过时的结果文案

### 7.2 代码与构建验收

- 通过：
  - 
pm test -- ProductWorkspaceQaSection.test.tsx
  - 
pm run build

### 7.3 真浏览器验收

- 路径：
  - http://127.0.0.1:5175/
  - 三个和尚 -> 正式工作台 -> QA 修复
- 实际操作：
  - 在真实 QA 工单详情中点击 触发复检
- 页面结果：
  - 页面无需手动刷新，即可回写新的 QA 结论
  - 本次真实数据下，页面落到：该集 QA 复检已完成，当前仍有 5 个问题待处理。
- 验收判断：
  - QA 工单动作已经不是“只发请求不收口”的半链路
  - 前端已具备真实复检任务的自动跟进与状态回流能力

### 7.4 当前仍需继续推进

- 当前 QA 生命周期 待处理 / 修复中 / 已解决 / 不修复 仍主要是前端本地记录，尚未完全落到后端真实工作流。
- 若后续继续推进 QA 模块，下一优先级应是：
  - 生命周期状态后端化
  - 修复版本 / 修复备注与真实版本记录绑定
  - QA issue 级别的更多状态细分（如 echeck_failed 可视化）

## 8. QA 生命周期后端化与真实表单持久化验收（2026-07-17）

### 8.1 本轮补齐

- 后端新增真实工单接口：
  - PATCH /api/books/{bookId}/qa/issues/{issueId}/workflow
- 本次不直接篡改自动修复状态机，而是将以下人工修复信息写入真实 QAIssue.meta_info：
  - workflow_status
  - epair_version
  - 
ote
- qa/workbench 返回已补充以上字段，前端不再只依赖浏览器本地 repairState。
- QA 页面现已支持：
  - 生命周期状态真实持久化
  - 修复版本真实持久化
  - 修复备注真实持久化

### 8.2 代码与测试验收

- 后端通过：
  - python -m unittest tests.test_qa_workbench_flow -q
- 前端通过：
  - 
pm test -- productWorkspaceQa.test.ts ProductWorkspaceQaSection.test.tsx
  - 
pm run build

### 8.3 真浏览器验收

- 路径：
  - http://127.0.0.1:5175/
  - 三个和尚 -> 正式工作台 -> QA 修复
- 实际操作：
  - 将选中真实工单状态改为 已解决
  - 填写修复版本 qa-manual-v3
  - 填写修复备注 真实页面验收：二次刷新后仍保留。
  - 刷新页面后重新进入项目与 QA 修复页
- 页面复验结果：
  - 刷新前：
    - status = resolved
    - ersion = qa-manual-v3
    - 
ote = 真实页面验收：二次刷新后仍保留。
  - 刷新后再次进入：
    - status = resolved
    - ersion = qa-manual-v3
    - 
ote = 真实页面验收：二次刷新后仍保留。
- 验收判断：
  - QA 生命周期与修复记录已从前端本地状态升级为真实后端持久化链路
  - 真浏览器已验证“修改 -> 刷新 -> 重进页面”后状态仍可恢复

## 9. 导出中心补齐“改编方向未锁定”交付阻塞（2026-07-17）

### 9.1 本轮修正

- 修正导出中心 readiness 逻辑：
  - 项目级改编方向未锁定时，导出中心必须视为真实阻塞
  - 阻塞清单新增：项目改编方向未锁定
  - 阻塞项 去处理 可直接跳转到 改编方向
- 这样可避免出现“上游主方向未锁定，但导出中心只提示图片/视频不足”的链路失真。

### 9.2 代码与测试验收

- 通过：
  - 
pm test -- productWorkspaceDelivery.test.ts
  - 
pm run build

### 9.3 真浏览器验收

- 路径：
  - http://127.0.0.1:5175/
  - 三个和尚 -> 正式工作台 -> 导出中心
- 页面结果：
  - 阻塞总数 从原先的 3 变为 4
  - 阻塞清单 中可见：项目改编方向未锁定
  - 点击该阻塞项的 去处理 后，可真实跳转到 改编方向
- 验收判断：
  - 导出中心已与项目级主方向锁定状态重新对齐
  - 交付闸门不再遗漏“主方向未锁定”这一全局阻塞项

## 10. 导出中心历史交付记录补齐“阻塞回溯”入口（2026-07-17）
### 10.1 本轮补齐

- 为历史交付记录新增结构化“阻塞回溯”动作区。
- 当前记录在写入 `metaInfo` 时，会同步保存 `blocked_codes`，不再只保留阻塞文案。
- 历史记录展示时，会优先按 `blocked_codes` 还原修复入口；老记录没有结构化字段时，再按阻塞文案关键词兜底识别。
- 已覆盖的回跳入口包括：
  - 改编方向
  - 剧本工作台（补剧本 / 锁稿 / 放行）
  - 镜头工作台（补提示词 / 补参考图 / 补视频）
  - 资产中心
  - QA 修复
- 额外兼容旧交付快照中的历史文案，例如：
  - `缺首帧` -> 回到镜头工作台补参考图
  - `未通过验收` -> 回到 QA 修复

### 10.2 代码与测试验收
- 通过：
  - `npm test -- productWorkspaceDelivery.test.ts`
  - `npm run build`

### 10.3 真浏览器验收

- 路径：
  - `http://127.0.0.1:5175/`
  - `三个和尚 -> 正式工作台 -> 导出中心`
- 页面结果：
  - 最新历史交付记录已出现 `阻塞回溯` 区块。
  - 历史阻塞记录可见真实修复按钮：
    - `返回剧本工作台锁定`
    - `返回分镜工作台补参考图`
    - `返回分镜工作台补视频`
    - `返回 QA 工作台修复`
  - 旧版 `2026-06-29` 交付快照中，`缺首帧` 也已能正确映射为 `返回分镜工作台补参考图`。
- 实际点击验证：
  - 点击 `返回剧本工作台锁定` 后，页面进入 `剧本工作台 -> 分集剧本列表`
  - 点击 `返回 QA 工作台修复` 后，页面进入 `QA 修复 -> QA 问题列表`
  - 点击 `返回分镜工作台补参考图` 后，页面进入 `镜头工作台 -> 镜头列表`

### 10.4 验收判断

- 导出中心不再只是“展示历史记录”，而是已经具备把用户带回真实修复工作区的闭环能力。
- 新老交付记录都具备可追溯、可回跳、可修复的操作入口，符合蓝图中的交付闭环要求。

## 11. 模型管理补齐统一适配配置与能力字段补全（2026-07-17）
### 11.1 本轮补齐

- 重写正式工作台中的 `模型管理` 页面，使其不再只是展示默认模型名，而是同时展示：
  - 默认模型
  - 统一适配层
  - 配置来源
  - 参考图/首尾帧/音频/负向提示词/异步任务能力
  - Provider 轮询与结果回收方式
- 新增 `供应商适配配置` 区块，明确说明业务页不会直接硬编码供应商差异，而是统一通过模型注册表 + 适配层处理。
- 修正了当前项目里 PoYo 默认模型能力字段老旧导致的展示偏差：
  - 之前页面会把 `PoYo GPT Image 2` 显示为不支持 `图片 URL / 负向提示词`
  - 也会把 `PoYo Happy Horse 1.1` 的部分能力展示得不完整
- 现在页面会对已知 PoYo 模型按预设自动补齐缺失字段进行展示，并标记：
  - `配置来源：按已知模型预设补齐`
- 同时新增显式动作入口：
  - `补齐当前默认模型能力字段`
  - 该动作会把默认模型缺失的能力字段正式写回注册表，避免其他业务页继续读到旧字段。

### 11.2 代码与测试验收
- 前端通过：
  - `npm test -- productWorkspaceModels.test.ts modelRegistryModal.test.ts`
  - `npm run build`
- 新增/补齐测试覆盖：
  - 旧的 PoYo 默认模型缺少能力字段时，页面仍能推断并展示真实能力
  - `buildSyncedCapabilityParams(...)` 能补齐默认模型缺失的显式字段
  - 模型管理页继续保留 PoYo 预设、推荐默认与能力矩阵能力

### 11.3 真浏览器验收

- 路径：
  - `http://127.0.0.1:5175/`
  - `三个和尚 -> 正式工作台 -> 模型管理`
- 页面结果：
  - `图像 / Image -> PoYo GPT Image 2` 现在正确展示：
    - `图片 URL -> 支持`
    - `负向提示词 -> 支持`
    - `异步任务 -> 支持`
  - `视频 / Video -> PoYo Happy Horse 1.1` 现在正确展示：
    - `首帧支持 -> 支持`
    - `尾帧支持 -> 不支持`
    - `音频支持 -> 不支持`
    - `异步任务 -> 支持`
  - 页面新增：
    - `适配层：PoYo 异步适配层 + Provider 轮询回收`
    - `配置来源：按已知模型预设补齐`
    - `供应商适配配置`
    - `补齐当前默认模型能力字段`
- 验收判断：
  - 用户现在可以清楚看到当前文本 / 图像 / 视频模型
  - 用户现在可以清楚知道各模型支持哪些参考图方式
  - 供应商差异已经被正式收敛到模型管理页与统一适配配置说明中，不再要求业务页自行推断

## 2026-07-17 改编方向锁定链路与界面文案回归

### 本轮完成
- 真浏览器验证 `三个和尚 -> 改编方向 -> 选择候选 -> 锁定为主方向` 已可走通。
- 锁定后已同步反映到：`项目控制台`、`剧本工作台`、`镜头工作台`、`任务中心`、`导出中心`。
- 修复资产中心遗留旧文案分支：人物 stale prompt 资产的快捷动作统一为 `重生新版参考图`。
- 修复任务中心对改编方向状态的脆弱判断：从文案比对改为使用显式布尔状态。
- 修复 QA 修复页搜索输入占位文案，恢复为 `搜索镜头号或问题关键词`。
- 同步更新对应前端测试断言。

### 自动化验证
- `npm test -- ProductWorkspaceAssetsSection.test.tsx ProductWorkspaceQaSection.test.tsx productWorkspaceTasks.test.ts`
- `npm run build`

### 真浏览器验收结果
- 资产中心：存在 stale prompt 的人物资产卡片显示 `重生新版参考图`，未再出现 `补新版参考图`。
- QA 修复：顶部筛选工具条显示正确中文占位 `搜索镜头号或问题关键词`。
- 任务中心：`改编方向锁定` 显示为 `已完成 / 已锁定`。

### 当前仍是主要阻塞的真实问题
- 资产补全未完成：当前仍有大量人物/场景/道具缺参考图。
- 分镜产出未完成：第 1 集仅有少量已采纳分镜图与视频。
- QA 问题池仍大：当前仍有大量剧本/分镜衍生问题待修复。
