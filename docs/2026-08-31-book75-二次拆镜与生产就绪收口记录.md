# book 75 二次拆镜与生产就绪收口记录

日期：2026-08-31  
状态：已完成真实数据结构变更与受控重编译；未触发新的图片或视频生成

## 背景

`book 75 / 第 1 集 / 镜头 1` 已在先前拆镜后仍返回 `executability=blocked`。复核表明，问题不是模型或提示词长度，而是 3 秒导演过程仍包含三个连续视觉事件：慵懒待机、打哈欠/说话、视线转向空街。

## 执行结果

- 使用既有通用可拍性规划器重新保存拆镜草案；草案按三个独立视觉事件给出三个连续 3 秒段落。
- 经已授权的确认写入，原镜头 1 拆为新镜头 1、2、3；同集后续镜头顺延，集内镜头数由 24 增至 26。
- 写入时为三个结果镜头保存状态快照锚点：`597`、`598`、`599`，可用于结构变更前恢复。
- 三个新镜头分别以 `executability-resplit-recompile` 重编译；当前可拍性均为 `pass`。
- 原 H3 成片未被删除，已随顺延关系保留在当前镜头 6：
  `uploads/generated-media/book-75-generated-视频 3 v1-fcb3b31c33b5-db917cc7cee5d99c.mp4`。
- [x] 镜头工作台的视频结论改为“当前镜头采纳视频”，并明确媒体标题保留原始生成审计；拆镜顺延后不再把历史标题误当作当前镜头号。

## 验收证据

- `GET /api/books/75/production-readiness`：`blocked_items: 0`，全书状态由 `blocked` 降为 `warning`。
- `python -m unittest tests.test_shot_executability tests.test_storyboard_structure tests.test_production_readiness tests.test_production_repair_plan`：19/19 通过。
- H3 视频关联仍为 `asset_status=done`，并保留两张锁定多参考图、供应商任务审计及本地持久化哈希。
- `npm --prefix web test -- ProductWorkspaceStoryboardSection.test.tsx`：16/16 通过；`npm --prefix web run build`：通过。
- `npm run check:production`：后端 Prompt/Compiler 生产回归 127/127 通过，前端生产构建通过；五项目 `audit:storyboard:zero-error-gate` 覆盖 125 个真实镜头，结果为 `0 error / 0 warning`。

## 剩余风险（未自动修改）

- [x] 经已锁定资产的操作者确认，5 个角色资产状态已从 `draft` 升为 `ref_ready`；未替换参考图、未改变人物外观。生产就绪 warning 已从 30 降为 27。
- “神秘男人”参考图仍为 `selected` 而非 `locked`；这是视觉确认决策，系统不得自动锁定或猜测。
- 场景“便利店收银台”的光线字段含有应属于镜头层的动作事实。
- “神秘女人”的服装信息同时写入 `hair_style`；正确的服装/发型归属需要人工确认后通过资产编辑保存，不能由关键词规则删除。
- 24 个镜头仍有 Prompt Compiler `warning`；它们不是可拍性阻塞，但应通过受保护的批量重编译与人工抽检逐步收口。
- 镜头 11、24 为 `executability=warning`，真实视频提交前仍需优化、拆镜或操作者明确覆盖并留存理由。
