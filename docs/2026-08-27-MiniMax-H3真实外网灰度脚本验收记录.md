# 2026-08-27 MiniMax H3 真实外网灰度脚本验收记录

## 结论

本轮已新增 MiniMax H3 真实账号灰度脚本第一版。

该脚本的核心目标不是直接跑真实视频，而是把“能否安全跑第一个真实样本”变成可审计的预检报告和强门禁 SOP。

## 新增入口

- 脚本：`scripts/validate-minimax-h3-gray.py`
- npm 命令：`npm run validate:minimax-h3-gray`

默认用法：

```powershell
npm run validate:minimax-h3-gray -- 75 1 1
```

自动选择候选镜头：

```powershell
npm run validate:minimax-h3-gray -- auto
```

自动候选会优先选择：

- 已有采纳首帧
- 首帧是外部可访问 URL
- 尽量没有既有视频版本
- H3 prompt 不含高风险坠落 / 伤害动作
- H3 prompt 尽量不含 `画面开场 / 画面切 / 请生成` 等遗留导演切口标记

该命令只做 dry-run：

- 读取真实 `book / episode / shot`
- 调用只读机器提示词导出 API
- 抽取 MiniMax H3 `integrated_multimodal_description`
- 检查 prompt 长度、首帧、视频模型配置、API key readiness
- 写出 `artifacts/minimax-h3-gray-*.json|md`
- 不登记任务
- 不提交 MiniMax
- 不写回视频资产

## 真实提交门禁

真实模式必须同时满足：

- CLI 显式参数：`--allow-real`
- 环境变量：`MINIMAX_H3_GRAY_REAL=1`
- 环境变量：`MINIMAX_H3_GRAY_CONFIRM=CONFIRM_MINIMAX_H3_SUBMIT`
- 环境变量：`MINIMAX_H3_GRAY_WHITELIST` 精确包含目标，例如 `75:1:1`
- 当前默认视频模型或显式 `--model-profile-id` 必须是 `minimax-h3-async`
- metaso MiniMax H3 兼容 API base URL 与 API Key 必须配置完成：`https://metaso.cn/api/minimax` + `mk-...`
- H3 prompt 不为空且不超过 7000 字
- 首帧图生视频必须使用 provider 可公网拉取的首帧 URL；本地 `/api/prototyping/assets/...` 或已 404 的外部 URL 会阻断真实提交

真实模式走正式后端链路：

1. `POST /api/books/{book_id}/storyboard/{episode}/{shot_id}/machine-prompt-api-submissions`
2. `POST /api/prototyping/tasks/{task_id}/submit-machine-prompt-provider`
3. `GET /api/prototyping/tasks/{task_id}`

因此真实灰度能同时验证：

- 机器提示词导出
- API 提交登记
- MiniMax H3 provider submit
- provider polling
- 视频资产回写
- 任务中心可追踪状态

## 本地 dry-run 验证结果

命令：

```powershell
npm run validate:minimax-h3-gray -- 75 1 1
```

结果：

- 脚本执行成功。
- 模式为 `dry-run-preflight`。
- 未触发 provider call。
- 未写回 storyboard asset。
- 报告指出当前默认视频 profile 仍是 PoYo，不是 `minimax-h3-async`，因此真实提交被正确阻断。
- 报告指出 `book 75 / episode 1 / shot 1` 当前没有采纳首帧，若真实提交会退化为文生视频。
- 生成的 artifacts 已按项目约定清理，不入库。

自动候选命令：

```powershell
npm run validate:minimax-h3-gray -- auto
```

结果：

- 脚本执行成功。
- 自动扫描 5 个真实项目，发现 6 个带采纳首帧的候选。
- 当前选择 `book 14 / episode 1 / shot 1`。
- 该镜头曾被初版脚本选中，因为它记录了外部 CDN 首帧、没有既有视频版本、没有灰度安全风险动作。
- 2026-08-28 复核发现该首帧 GET 返回 `404`，因此不再具备真实 H3 图生视频灰度条件。
- 2026-08-27 首轮报告提示该镜头 H3 prompt 仍含 `画面开场 / 画面切` 类遗留导演标记，真实提交前建议先人工复核或修正提示词。
- 生成的 artifacts 已按项目约定清理，不入库。

## 2026-08-28 补充复验：机器提示词导演口令清洗

本轮已把 H3 导出前的机器字段清洗补到 `compile_machine_prompt`：

- 导演分镜语言 `director_shot_text` 保持原样，继续作为用户可编辑的创作层文本。
- 模型提交字段会清理 `画面开场 / 镜头开场 / 画面切 / [画面` 等剪辑口令，并尽量保留可观察动作内容。
- H3 WebUI 与通用中文视频 WebUI 导出均使用清洗后的机器字段。

复验命令：

```powershell
python -m unittest tests.test_machine_prompt_export tests.test_model_adapter
python -m py_compile scripts/validate-minimax-h3-gray.py
npm run validate:minimax-h3-gray -- auto
```

结果：

- Python 单测通过：13 tests。
- H3 灰度脚本通过 dry-run，未触发 provider call，未写回 storyboard asset。
- 自动候选仍为 `book 14 / episode 1 / shot 1`。
- 报告 `readiness.warnings = []`。
- 当前唯一真实提交 blocker 为 `video_profile_is_not_minimax_h3_async`。

## 2026-08-28 补充：真实提交前摘要

为进入 metaso MiniMax H3 真实账号灰度，正式镜头工作台已在“机器提示词导出预览”内新增真实提交前摘要。

摘要用于在用户点击“真实提交 H3”前确认：

- 平台与 Base URL：`metaso.cn MiniMax H3 兼容 API` / `https://metaso.cn/api/minimax`
- 模型：`MiniMax-H3`
- 默认规格：分辨率 `768P`，时长由当前分镜 `duration` 决定，并按 H3 平台范围夹取到 `4s–15s`，画幅 `16:9`
- 时长来源：摘要会明确显示来自分镜、平台夹取还是脚本命令行覆盖
- AIGC 水印：默认关闭
- Prompt 长度
- 当前会走首帧图生视频还是文生视频
- 如果没有采纳首帧，会提醒后端将按文生视频提交，建议先补齐首帧再灰度

该摘要不替代后端门禁；真实提交仍必须通过确认口令、`minimax-h3-async` profile、API Key 与白名单灰度脚本。

## 2026-08-28 补充：视频时长跟随分镜

本轮已取消 H3 真实提交链路里的固定 `5s` 假设。

- 正式工作台真实提交 H3 时，`durationSeconds` 从当前镜头分镜 `duration` 推导。
- 灰度脚本默认同样读取分镜时长；只有显式传入 `--duration-seconds` 时才覆盖分镜。
- 推导结果会写入摘要和报告：有效分镜时长直接使用，超出 H3 平台范围时夹取到 `4s–15s`，缺失或非法时长使用默认 `5s`。

## 2026-08-28 补充：七牛首帧公网中转

本轮发现旧候选 `book 14 / episode 1 / shot 1` 的采纳首帧虽然记录为外部 CDN URL，但 GET 实测返回 `404`，不具备 H3 图生视频真实提交条件。

已补充：

- 灰度脚本自动候选会实际检查首帧 URL 是否可访问，不再只按 URL 形态判断。
- H3 真实提交入口在提交 provider 前会验证首帧 URL；不可访问时，如果配置了七牛公网资产中转且源图可读取，则上传到七牛并使用签名 URL。
- 已经失效的外部 URL 无法被七牛恢复，必须重新生成或重新上传首帧。
- 当前七牛 Bucket `ai-ku01` 位于华南-广东（`z2`），为私有空间；测试域名不支持 HTTPS 且 30 天回收，仅用于临时开发。

## 下一步

- 在模型管理中配置 metaso MiniMax H3 `minimax-h3-async` profile：`base_url=https://metaso.cn/api/minimax`，`model_name=MiniMax-H3`，API Key 使用 `mk-...`。
- 使用 `npm run validate:minimax-h3-gray -- auto` 选择第一条候选；当前脚本会把不可公网访问首帧作为 blocker。
- 配置视频 profile、API Key 和七牛中转后，用脚本 dry-run 生成报告并确认无 blockers。
- 用户明确批准后，再按报告里的 PowerShell SOP 跑真实提交。
