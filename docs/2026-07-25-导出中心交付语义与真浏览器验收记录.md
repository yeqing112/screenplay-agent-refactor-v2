# 2026-07-25 导出中心交付语义与真浏览器验收记录

## 1. 本轮目标

继续按《产品重构蓝图》推进导出中心，重点收口两类问题：

1. 导出工具层中的历史乱码语义会污染交付摘要、阻塞回溯、导出文档标题与 QA 摘要。
2. 导出中心必须在真浏览器里确认已经呈现为正式产品语义，而不是仅靠单测通过。

## 2. 本轮改动

### 2.1 重写导出工具层

修复文件：

- `web/src/components/productWorkspaceDelivery.ts`

处理内容：

1. 重建导出 readiness 语义：
   - `可交付`
   - `阻塞中`
   - `待补齐`
2. 重建交付阻塞项语义：
   - 项目改编方向未锁定
   - 缺剧本
   - 剧本未锁稿
   - 剧本未放行
   - 缺镜头
   - 提示词未齐
   - 缺已采纳分镜图
   - 缺已采纳视频
   - 缺可追溯资产引用
   - QA 阻塞
3. 重建交付历史摘要与阻塞回溯动作文案。
4. 重建导出摘要、Word 导出文档、Final Draft 导出文档的正式标题与章节文案。
5. 保留原有接口契约与导出数据结构，不改变业务调用方式。

### 2.2 同步更新测试基线

修复文件：

- `web/src/components/productWorkspaceDelivery.test.ts`

处理内容：

1. 将旧乱码断言切换为正式中文语义断言。
2. 继续覆盖：
   - readiness 判定
   - 历史阻塞动作回推
   - 导出包结构
   - Word / Final Draft 导出文档

## 3. 自动化验收

### 3.1 导出模块专项

执行命令：

```bash
npm --prefix web run test -- productWorkspaceDelivery.test.ts
```

结果：

- `1` 个测试文件通过
- `8` 条测试全部通过

### 3.2 交叉回归

执行命令：

```bash
npm --prefix web run test -- productWorkspaceDelivery.test.ts productWorkspaceTaskCenterData.test.ts ProductWorkspaceAssetsSection.test.tsx productWorkspaceAssetActions.test.ts
npm --prefix web run test -- productWorkspaceTaskRecoveryActions.test.ts productWorkspaceTaskCenterState.test.ts productWorkspaceTasks.test.ts
npm --prefix web run build
```

结果：

1. 第一组：
   - `4` 个测试文件通过
   - `23` 条测试全部通过
2. 第二组：
   - `3` 个测试文件通过
   - `12` 条测试全部通过
3. `vite build` 通过

说明：

- 导出中心这轮修复没有破坏任务中心、资产中心与任务恢复链路。

## 4. 真浏览器验收

验收日期：

- 2026-07-25

验收地址：

- 前端：`http://127.0.0.1:5182`

验收路径：

1. 打开首页
2. 进入项目 `三个和尚`
3. 点击 `正式工作台`
4. 切换到 `导出中心`

### 4.1 页面可见结果

真页面中已确认出现以下正式语义：

1. 顶层页签：
   - `导出中心`
2. 导出指标卡：
   - `交付集数`
   - `可交付`
   - `待修复`
   - `阻塞总数`
3. readiness 区域：
   - `交付 readiness`
   - `第 1 集`
   - `阻塞中`
4. 面板与阻塞：
   - `第 1 集交付面板`
   - `阻塞清单`
   - `缺已采纳分镜图`
   - `缺已采纳视频`
   - `QA 87 项`
5. 交付包区域：
   - `交付包内容`
   - `采纳分镜图`
   - `采纳视频`
   - `人物参考图`
6. 交付动作区：
   - `记录阻塞快照`
   - `导出 JSON 并登记`
   - `导出 Word`
   - `导出 Final Draft`
   - `导出 PDF`
   - `复制交付摘要`
7. 历史记录区：
   - `交付记录`
   - `历史记录会优先显示清洗后的摘要，旧脏文案会自动回退到标准格式。`

### 4.2 页面稳定性判断

验收结论：

1. 页面已可见正式中文交付语义。
2. 本轮真浏览器验收未发现新的 `?????` 或大面积乱码回归。
3. 导出中心已不再只是“技术快照页”，而是更接近蓝图中的正式交付面。

验收截图：

- `artifacts/acceptance-2026-07-25-delivery-center-current.png`

## 5. 结论

本轮已经完成以下收口：

1. 导出工具层正式脱离历史乱码语义。
2. 交付摘要、阻塞项、阻塞回溯、导出文档标题已统一到同一套正式中文语义。
3. 自动化测试、构建和真浏览器验收三层全部通过。

## 6. 下一步建议

后续继续按蓝图推进，优先级建议如下：

1. 继续推进提示词编译治理：
   - 静态资产引用
   - 角色多变体继承
   - 场景 / 道具多变体继承
2. 继续做正式工作台全局真浏览器巡检：
   - 内容准备
   - 改编方向
   - 剧本工作台
   - 镜头工作台
   - 任务中心
   - 导出中心
3. 继续推进“从小说到分镜图再到交付”的整链路真实操作验收
