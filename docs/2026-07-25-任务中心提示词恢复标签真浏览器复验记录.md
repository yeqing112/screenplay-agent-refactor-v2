# 2026-07-25 任务中心提示词恢复标签真浏览器复验记录

## 本轮目标

确认“镜头工作台 -> 重新编译提示词 -> 任务中心恢复链路”已经在真实浏览器中正确显示为“提示词编译结果回收”，不再出现“参考图结果回收”或“?????结果回收”。

## 验收环境

- 日期：2026-07-25
- 前端：`http://127.0.0.1:5182`
- 后端：`http://127.0.0.1:18769`
- 浏览器：Playwright 真页面操作
- 项目：`三个和尚`
- 正确项目 ID：`14`

## 验收步骤

1. 打开正式产品工作台并进入项目 `三个和尚`（ID `14`）。
2. 切换到“镜头工作台”。
3. 选中第 1 集第 1 镜 `寺庙后院水房`。
4. 点击“重新编译提示词”。
5. 确认本地待恢复存储 `product-workspace.pending-storyboard-tasks.14` 新增：
   - `taskId`
   - `episode = 1`
   - `shotId = "1"`
   - `kind = "prompt"`
6. 切换到“任务中心”。
7. 检查新增恢复任务的标题文案与总任务数。

## 实际结果

- 已成功写入本地待恢复任务：
  - `taskId = 0f7333c33bbd`
  - `episode = 1`
  - `shotId = "1"`
  - `kind = "prompt"`
- 任务中心总任务数从 9 增加到 10。
- 新增恢复任务显示为：
  - `提示词编译结果回收`
  - `第 1 集 · 1 · 寺庙后院水房`
  - `45%`
- 未再出现：
  - `参考图结果回收`
  - `?????结果回收`

## 结论

本项通过验收。

“提示词异步重编译任务 -> 本地 pending 存储 -> 任务中心恢复任务展示”链路已经打通，且用户可见标签已修正为正确的提示词恢复语义。

## 本轮补充改动

- 修复 `web/src/components/productWorkspaceRecovery.ts` 中恢复类型标签，改为稳定的 Unicode 转义写法，避免终端编码污染源码。
- 新增前端测试：
  - `web/src/components/productWorkspaceTaskCenterData.test.ts`
  - 覆盖 `kind = "prompt"` 时的任务标题、目标镜头与进度显示。
