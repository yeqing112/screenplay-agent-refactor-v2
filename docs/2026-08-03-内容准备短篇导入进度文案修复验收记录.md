# 2026-08-03 内容准备短篇导入进度文案修复验收记录

更新时间：2026-08-03

## 1. 验收目标

验证正式产品工作台中的 `内容准备` 链路，在真实浏览器下从“新建项目”进入“短篇录入”后：

- 导入进度文案不再出现乱码
- 阶段摘要与项目状态文案可读
- 导入完成后能够稳定进入 `可继续改编方向` 的状态

## 2. 本轮修复范围

本轮针对以下问题做了收口：

1. `web/src/components/productWorkspaceUpstreamController.ts`
   - 重建内容准备控制器
   - 修复短篇导入、上传导入、轮询超时、错误回退等用户可见文案
   - 修复短篇正文集数估算正则
   - 增加 `contentTask.message` 的脏字符串清洗
   - 增加内容准备阶段步骤与 warning 的用户向中文映射

2. `web/src/components/productWorkspaceUpstream.ts`
   - 重建内容准备 / 改编方向摘要文案

3. `web/src/components/productWorkspaceUpstream.test.ts`
   - 重建测试并改为干净文案断言

4. `web/src/components/productWorkspaceProjectStatus.ts`
   - 修复 `bibeled` 等状态映射
   - 将 `已完成 Bible` 调整为 `已完成小说圣经`

## 3. 自动化回归

执行结果：

```bash
cd web
npm run test -- ProductWorkspaceContentSection productWorkspaceContentModels productWorkspaceUpstream
npm run test -- productWorkspaceUpstream productWorkspaceProjectStatus
npm run build
```

结果：

- `vitest` 通过
- `vite build` 通过

## 4. 真实浏览器验收路径

验收环境：

- 前端：[http://127.0.0.1:5175](http://127.0.0.1:5175)
- 后端：[http://127.0.0.1:18765](http://127.0.0.1:18765)
- 日期：2026-08-03

真实操作路径：

1. 打开首页
2. 点击 `新建项目`
3. 进入正式产品工作台
4. 打开 `内容准备`
5. 在“添加短篇小说”中填写标题与正文
6. 点击 `创建短篇并导入`
7. 观察处理中、完成态、项目状态与 warning 文案

## 5. 验收结果

### 5.1 首次复测

短篇导入后，原先的乱码：

- `瀵煎叆鏂囦欢 / 14%`

已修复为：

- `导入文件 / 14%`

### 5.2 二次复测

进一步观察轮询中的后续步骤后，补充修复了旧任务步骤名映射。

最终复测时，页面未再出现：

- `瀵`
- `閫`
- `Bible`
- `No character signals found`

### 5.3 最终完成态实测结果

真实浏览器最终看到的关键信息为：

- `当前项目内容状态：已完成`
- `当前已整理出 1 章、约 0.0 万字内容，可进入改编方向。`
- `项目状态：已完成小说圣经`
- `导入状态：已完成`
- `内容准备完成 / 100%`
- `人物画像已跳过：未识别到可用人物信号，请先确认逐章分析结果是否完整。`

## 6. 验收结论

本轮内容准备短篇导入前链路验收通过。

已确认：

- `新建项目 -> 内容准备 -> 短篇录入 -> 导入处理中 -> 导入完成`
  这条真实用户链路可跑通
- 进度文案、步骤文案、状态文案已从乱码恢复为可读中文
- 导入完成后可以正确放行到改编方向

## 7. 剩余关注项

本轮已收敛核心乱码问题，但仍建议后续继续推进：

1. 后端 `api/server.py` 的内容准备步骤名统一中文化，减少前端依赖清洗词表
2. 继续对正式工作台其他高频链路做同类真浏览器文案巡检
3. 按编码治理规范继续清理历史脏字符串来源
