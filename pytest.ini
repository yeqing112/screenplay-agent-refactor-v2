[pytest]
# Root-level test_*.py files in this repository are manual pipeline scripts
# (some mutate sample books and call real QA/model code). Keep the automated
# regression boundary explicit so `pytest` is deterministic and cannot execute
# those scripts during collection.
testpaths = tests
python_files = test_*.py
addopts = -ra
